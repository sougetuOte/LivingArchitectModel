# NFR-14a 文脈確認（2026-06-20）

## artifact §1 引用

### v5 構想 ④ fat 監査レポート 表引用（行 30）

```
| NFR-14a | フック分類誤判定率: Wave 1 完了後に計測 | 無（計測実装・ベースライン未確立） | 無 | low | medium | 要追加調査 | v5 で計測基盤構築タスクへ再起票（Opus 査定で降格・元 Sonnet 判定: 削除候補）|
```

### 詳細セクション（行 219-229）

```
#### NFR-14a: フック分類の誤判定率を Wave 1 完了後に計測し、ベースラインを確立する

- **定義**: Wave 1（権限等級システム）完了後に誤判定率を実測してベースラインを設定する
- **集計実装**: 無。`docs/tasks/v4.0.0-immune-system-tasks.md:425` に「AC-1.35: Wave 1 完了後にフック分類の誤判定率を計測する手順が記載されている」チェックが入っているが（完了マーク `[x]`）、実際の計測スクリプト・測定結果ファイルは存在しない。`docs/design/v4.0.0-immune-system-design.md:176` に「Wave 1 完了後の誤判定率計測でチューニング」の記述のみ
- **出力先**: 無（計測結果ファイル・スクリプトなし）
- **使用箇所**: `docs/specs/v4.0.0-immune-system-requirements.md:564,675`、`docs/tasks/v4.0.0-immune-system-tasks.md:425`
- **最終更新**: tasks.md の該当チェック - コミット不明（Wave 1 完了時）
- **使用頻度**: low（「計測する手順が記載されている」のみ達成。実計測なし）
- **失敗時影響**: medium（誤判定率不明のまま運用中。ただし「迷ったら SE」原則でフェイルセーフ）
- **マトリクス分類**: 要追加調査（Opus 査定で降格・元 Sonnet 判定: 削除候補。実態は「定義更新を伴う再起票」であり撤去でなくリファクタのため）
- **削減プラン**: NFR-14a の定義を「Wave 1 完了後」から「v5 Phase 1」へ更新し、計測スクリプト実装タスクを v5 タスクとして再起票（PM 級）。現 NFR-14a はフラグとして温存しつつ「未達成」を明記
```

## artifact 内 "再起票" 系キーワード grep

ヒット: 3 件

1. **行 30**（表内）: `v5 で計測基盤構築タスクへ再起票（Opus 査定で降格・元 Sonnet 判定: 削除候補）`
2. **行 229**（詳細説明）: `計測スクリプト実装タスクを v5 タスクとして再起票（PM 級）`
3. **行 1094**（§2 統計）: `NFR-6/7/8 削除 + NFR-14a 再起票`

**対象セクション**: §1.2 詳細の「削減プラン」フィールドに「再起票」と明示、§2 統計サマリに「再起票」として分類。

## v4.0.0 NFR-14a 該当箇所（行 564 前後）

### 該当行 564 及び前後 30 行

```
### 6.4 セキュリティ

| ID | 要件 |
|----|------|
| NFR-10 | フック障害時は `ask`（確認要求）に劣化する（全操作停止にしない） |
| NFR-11 | 自動生成ルールは人間承認なしに有効化されない（MVP） |
| NFR-12 | PG 権限で実行可能なコマンドの上限を `settings.json` で明示 |
| NFR-13 | `.claude/rules/auto-generated/` の変更は git diff で追跡可能 |
| NFR-14a | フック分類の誤判定率を Wave 1 完了後に計測し、ベースラインを確立する |

### 6.5 可観測性

| ID | 要件 |
|----|------|
| NFR-15 | 全フック判定のログ出力（等級、判定理由、実行結果） |
| NFR-16 | ループログの永続化（サイクル数、修正内容、収束理由） |
| NFR-17 | 運用KPI の定期集計（`/daily` コマンドとの連携） |
```

## 事実報告

### artifact での扱い

- **§1 表（行 30）**: 「要追加調査」に分類。削減プラン欄に「v5 で計測基盤構築タスクへ再起票」と明記
- **§1.2 詳細（行 219-229）**: 「定義」「集計実装」「出力先」を詳述。実装状況は「無（計測スクリプト・測定結果ファイル存在しない）」と明示。削減プラン欄に「定義を『Wave 1 完了後』から『v5 Phase 1』へ更新」「v5 タスクとして再起票（PM 級）」「フラグとして温存しつつ『未達成』を明記」と記載
- **§1.3 統計（行 1094）**: 「削除候補 4 / 温存 11 / 共通化 1 / 要追加調査 1」の「要追加調査 1」として計上。「NFR-6/7/8 削除 + NFR-14a 再起票」と列挙

### v4.0.0 での扱い

- **行 564** に NFR-14a が仕様テーブル内に記載
- **セクション分類**: 6.4「セキュリティ」に配置（NFR-10〜13 の後、NFR-15 以前）
- **要件文**: 「フック分類の誤判定率を Wave 1 完了後に計測し、ベースラインを確立する」と記載
- **拘束度**: テーブル上に拘束度列がない（他 NFR と異なり、単純な「要件」列に記載）。仕様書 6.4 全体を見ると「セキュリティ」カテゴリだが、拘束度の表示なし
- **状態**: 「定義」のみ。計測実装・達成条件・完了判定ロジックの記載なし（仕様書は設計・実装ロードマップに記載すべき事項）

## 整合性分析（事実のみ）

| 観点 | artifact | v4.0.0 | 状態 |
|------|---------|--------|------|
| 定義文 | 同一：「フック分類の誤判定率を Wave 1 完了後に計測...」 | 同一 | 一致 |
| 実装状況 | 「無（計測実装・ベースライン未確立）」と明示 | 要件のみ記載、実装状況記載なし | artifact が詳述・実装ギャップ可視化 |
| 分類 | 「要追加調査」（削除候補から降格） | 「セキュリティ」カテゴリ（拘束度表示なし） | 異なる視点。artifact は実装可能性視点、v4.0.0 は要件カテゴリ視点 |
| 対処 | 「v5 Phase 1 へ再起票」「PM 級」「フラグとして温存」と明記 | 記載なし | artifact が追加調査結果を反映した対処方針を記載 |
| 波及 | 「計測基盤構築タスク v5 へ」と展開 | Wave 1 からの延期判定のみ | artifact が次段階（v5）までの連鎖を記載 |

