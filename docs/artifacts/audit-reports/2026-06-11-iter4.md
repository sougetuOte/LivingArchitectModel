# 監査統合レポート（B-2 full-review イテレーション4）

**実施日**: 2026-06-11
**対象**: `.claude/hooks/`（ゼロベース再スキャン・4巡目）
**構成**: 監査6体（SRC-A/SRC-B/TEST-A/TEST-B/SEC/QA・sonnet）+ 監督査定（本体）
**Stage 1**: 静的解析 Issues 10 — **全件がルート直下 `verify_audit*.py`（前セッション残置の一時ファイル・監査対象外）由来のノイズ**。`.claude/hooks` 本体と gitleaks はクリーン

## サマリ

```
生指摘: 17件（SRC-A 4 / SRC-B 2 / TEST-A 5 / TEST-B 2 / SEC 1 / QA 3）
監督査定: Info 降格 3（SRC-A-1 / TEST-B-1 / TEST-B-2）
確定: Critical 0 / Warning 9（全件修正済み）/ PM級 1（仕様補記・承認待ち）/ Info 6
修正: Warning 9件 + Info 自明修正 1件（計10）
検証: 743 passed（738 + 新規テスト5・回帰ゼロ）/ ruff clean
Before=17 > 0 のため Green State 未達。PM 決着後にイテレーション5（最終・max_iterations=5）で再スキャン
```

## 確定 Warning（全件修正済み）

| # | 等級 | 箇所 | 内容 | 修正 |
|---|------|------|------|------|
| W4-1 | SE | `lam-stop-hook.py:288` | `_check_context_pressure` の mtime フォールバック最内側 `except Exception: pass` がログなし黙殺。コンテキスト圧迫の検知失敗が観測不能（iter1 W-2 同型） | `_log(WARN)` 追加 |
| W4-2 | SE | `post-tool-use.py:244` | `_handle_doc_sync_flag` の既存フラグ読取失敗黙殺。重複チェック無効化（同一パス重複追記）が観測不能 | `log_entry(WARN)` 追加（`log_file` 引数追加） |
| W4-3 | SE | `state_manager.py:132` | `load_ast_map` が JSONDecodeError は `{}` フォールバックするのに、フィールド欠損 KeyError・型不正は未捕捉伝播（防御の非対称・iter3 OSError 対称性先例と同系統） | `except (KeyError, TypeError, AttributeError)` + warning + `{}`。TDD（Red→Green）テスト2件追加 |
| W4-4 | SE | `checkers/check_g1_test.py:63` | package.json 読取の `except Exception` が過広域（pyproject 側は `except OSError` 限定と非対称） | `except (OSError, ValueError)` に狭域化（JSONDecodeError/UnicodeDecodeError は ValueError 系。「PASS 時 stdout/stderr 無出力」契約のためログなし縮退は維持） |
| W4-5 | SE | `_hook_utils.py:245` | `run_command()` が本番・テスト両方で呼出ゼロの Dead Code（SEC-1 Low 起票 → Dead Code として確定。iter2 W2-3 `get_tool_response` 削除先例と整合）。`os.environ` 全継承の潜在リスクも削除で同時解消 | 関数 + 専用 import（`shutil`/`subprocess`）を削除 |
| W4-6 | SE | `tests/test_pre_tool_use.py` | `_sanitize_target` の Cc/Cf 制御文字除去（Trojan Source 対策・セキュリティロジック）にテストなし（guideline「テストが存在しない新規ロジック」） | U+202E + C0 制御文字の置換検証テスト追加（ソースには `chr(0x202E)` を使用し生の双方向制御文字を埋め込まない） |
| W4-7 | SE | `tests/test_post_tool_use.py` | `_parse_junit_xml` の `<testsuites>` ルート分岐（Jest/Maven 形式・複数スイート合算）にテストなし | 2スイート合算（tests/failures/errors/failed_names）のユニットテスト追加 |
| W4-8 | SE | `tests/test_stop_hook.py` | PreCompact フラグ閾値（600秒）**超過**時のループ継続パスにテストなし（直近フラグ→停止の片側のみ検証済み。閾値条件の反転バグを検知できない） | 1時間前フラグで block 継続 + 状態ファイル残存の検証テスト追加 |
| W4-9 | SE | `lam-stop-hook.py:404` (QA-1) | `_apply_g1_result` に「Wave 2 まで G1 のみ」の注記がなく、design D3（G1/G2/G5 全実行)との乖離がコードから判断不能 | docstring に Wave 2 スコープ注記 + `stop_hook_active` はログ専用・iteration 上限が主 bound である設計意図を補記（SRC-A-1 の Info 対応を兼ねる） |

## Info（自明修正1件実施）

- SRC-A-4 [SE]: `post-tool-use.py` `_read_prev_result` の読取失敗黙殺に `log_entry(WARN)` 追加（W4-2 と同型。False=前回成功扱いへの保守的縮退自体は設計どおり維持）

## Warning→Info 降格（監督査定・記録のみ）

| 生指摘 | 降格理由 |
|--------|---------|
| SRC-A-1（`stop_hook_active` Dead Code・AUTONOMOUS 再帰防止未実装） | 変数はログ出力に使用されており Dead Code ではない。再帰 bound は `_handle_autonomous` の iteration 上限が主 bound（docstring に明記済みの意図的設計）。G1 checker の毎回実行は「checker 接地で completion を gate する」AUTONOMOUS 設計の核心。設計意図の可視化のみ W4-9 の docstring 補記でカバー |
| TEST-B-1（`_count_inline_issue_markers` ヘルパーのユニットテスト欠如） | テストヘルパー自体へのユニットテスト要求は過剰（guideline アンチパターン「過剰な抽象化要求」相当）。fixture からの期待値導出はハードコードより頑健であり「docstring 内マーカー誤カウント」は投機的 |
| TEST-B-2（`test_integration_pipeline.py:365` の `set()` 順序無視比較） | 順序付きラウンドトリップ検証は `test_state_manager.py:519`（`loaded["topo_order"] == ["c","b","a"]`）に既存。guideline「テストのアサーション追加提案（主要パスはカバー済み）は Info」に該当 |

## Info（記録のみ・修正不要・Green State 非阻害）

- TEST-A-4: `make test` の TDD 記録パステスト欠如（pytest/npm/go はカバー済み・検出レイヤーは別途テスト済み）
- TEST-A-5: env allowlist テストの必須キー検証が Unix 系変数のみ（実装には Windows 変数あり・現状問題なし）

## PM級 — 承認待ち（1件・仕様書への追記）

| # | 箇所 | 内容 | 提案 |
|---|------|------|------|
| PM4-1 | `docs/specs/loop-log-schema.md` §3.1 | §3.1 が定義する MVP テキストフォーマット（`--- Iteration 1 ---` / `Issues: Critical=...` 形式）と実装（`lam-stop-hook.py:96-116`: `--- Iteration Log ---` + `iter N: found=... fixed=...` 形式）が不一致。§3.2 の MVP マッピング追記（iter1 PM 決着）は `log[]` フィールド対応表でありテキストフォーマット定義を置換していない（QA-2/QA-3 統合） | §3.1 を実装の実フォーマットに更新し、旧形式は完全実装（Wave 2+）の目標形式として位置付け直す |

## 対象外ノイズ（参考）

- 静的解析 10 件は全てルート直下 `verify_audit2/3/4/7/8.py`（untracked・前セッションの検証用一時ファイル）の ruff 指摘。監査対象 `.claude/hooks` の範囲外。ファイル削除で消滅する（ユーザー判断待ち）

## 次アクション

1. PM4-1 をユーザーに提示（pm_pending=true・ループ一時停止）
2. PM 修正後、イテレーション5（最終）: Stage 2 ゼロベース再スキャン（Before=0 確認）

## PM4 決着記録（2026-06-11 追記）

ユーザー判断: **A 承認**（仕様を実装に合わせて更新。実装・テスト変更なし）

| # | 決着 | 反映箇所 |
|---|------|---------|
| PM4-1 | A 承認 | `loop-log-schema.md` §3.1 — フォーマット例を実装の実出力（`--- Iteration Log ---` + `iter N: found=... fixed=... pg=... se=... pm=... tests=...` の1行形式）に差し替え。フィールド意味は §3.2 マッピング表への参照で接続。旧構造化形式（Critical/Warning 内訳等）は「完全実装での目標形式（参考・未実装）」として残置し、MVP が1行形式を採用した理由（grep 集計容易性）を明記 |

`pm_pending=false` に更新済み。次: イテレーション5（最終）ゼロベース再スキャン。
