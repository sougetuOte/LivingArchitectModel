# 監査統合レポート（full-review イテレーション1）

**対象**: `.claude/hooks`（production 5,642行 + tests、計 15,872行）
**日付**: 2026-06-02
**Scale Detection**: Plan A のみ（15,872行・ruff/bandit 導入済み、tree-sitter なし→Plan B/C/D 無効）
**Stage 1 静的解析**: ruff Warning 4件（F401・全て tests/・auto-fix可）、bandit 0、gitleaks 0（シークレット漏洩なし）

## サマリー

| 重要度 | 件数 |
|--------|------|
| Critical | 2 |
| Warning | 15 |
| Info | 11 |

| 等級 | 件数（概算） |
|------|------|
| PG | 5 |
| SE | 16 |
| PM | 5 |

総合評価: **C**（Critical=2 / Warning=15。Green State 未達）

---

## Critical（2件）

両方とも `card_generator.py`（1,305行・⑤で2モジュール分割が計画済み）に集中。

- **[C-1][PM]** `analyzers/card_generator.py:886-912, 1010-1032` — `detect_circular_dependencies()` / `build_topo_order()` の戻り値が「循環なし」と「RecursionError による検出失敗」を区別不能。**サイレントな循環依存見逃し**。戻り値契約の明確化が必要（spec_ambiguity）。
  - ※ SESSION_STATE の「残 Critical（`[]` 戻り値が循環なしと区別不能）＝⑤分割時に対応」に一致。
- **[C-2][SE]** `analyzers/card_generator.py:831-876` — `_find_sccs()` が再帰 Tarjan 実装でノード数1000超時に RecursionError（コード内コメント L836-838 でも認識済み）。反復実装への置換が必要。
  - ※ 本プロジェクトの実 import グラフは数十ノードのため**実発火しない潜在バグ**。

## Warning（15件）

### src-quality（6件）
- **[W-1][SE]** `lam-stop-hook.py:334-384` — `main()` が50行制限超過（`_run_full_review_flow()` 抽出で分割可）。
- **[W-2][SE]** `lam-stop-hook.py:117` — `_save_loop_log()` の `except Exception: pass` がエラー黙殺（警告ログ化すべき）。【Error Swallowing】
- **[W-3][SE]** `pre-compact.py:35-60` — `update_session_state()` が OSError 未捕捉（ディスク満杯時にファイル破損リスク）。
- **[W-4][SE]** `card_generator.py:840` — `index_counter = [0]` の closure workaround → `nonlocal` 推奨。（※⑤送り候補）
- **[W-5][PM]** `lam-stop-hook.py:379-382` — `_SECRET_PATTERN` が `.md`/`.txt` の例示コードで偽陽性 → `sec_fail=True` でループ機能不全の可能性。
- **[W-6][PM]** `pre-tool-use.py:93-98` — `_FR9_PATTERNS` が `.claude/hooks/` 配下テストも DENY（AUTONOMOUS+G1 FAIL→building 戻り時に hooks/ テスト修正がブロックされる仕様齟齬の可能性）。

### test-quality（2件・いずれも既知）
- **[W-7][SE]** `tests/test_stop_hook.py:17` & `tests/test_loop_integration.py:21` — `DEFAULT_STATE` 2ファイル重複（`command` 値の微差あり）。conftest に共通ベース定数化で整理可。
- **[W-8][SE]** `analyzers/run_pipeline.py` — `_detect_analyzers()` / `_persist_results()` に直接ユニットテストなし（`run_phase0()` 統合経由のみ）。

### arch-spec（5件）
- **[W-9][SE]** `lam-stop-hook.py`（該当コードなし） — tdd-introspection-v2.md §2.1 の Stop hook「通知B」（未分析エントリ閾値→/retro 推奨）が未実装【spec_drift】。
- **[W-10][PM]** `checkers/` に `check_g1_test.py` のみ — autonomous-mode design D3 / FR-4.1a が要求する `check_g2_lint.py` / `check_g5_security.py` 未実装。AUTONOMOUS の Green State 判定が G1 のみ【spec_drift】。
- **[W-11][SE]** `analyzers/gitleaks_scanner.py:21` — `from .base import Issue`（相対 import）が他モジュールと不統一。`auto_discover()` のパッケージ無コンテキストロード時に ImportError リスク【structural】。
- **[W-12][SE]** `SKILL.md` Stage 0 / `scale_detector.py` `__main__` — `$TARGET` をサブディレクトリ指定時、`scale-detection.json` が `<TARGET>/.claude/review-state/` に、`run_pipeline` の出力は `<ROOT>/.claude/review-state/` に書かれ**不一致**【structural・本監査で実地確認済み】。
- **[W-13][PM→SE]** `SKILL.md` Stage 3 Step 2 コメント — `merge_contracts(file_cards, contract_fields, ast_map, module_to_files)` と記述だが実装（`card_generator.py:411`）は引数3・4が逆【spec_drift・要実地確認】。

### security（2件）
- **[W-14][SE]** `lam-stop-hook.py:268` — `_run_g1_checker` が `{**os.environ, ...}` で全環境変数をサブプロセス継承（ローカルでは実害なし、CI 展開時リスク）。`conftest.py::_ENV_ALLOWLIST` 方式を本番にも適用すべき。
- **[W-15][SE]** `_hook_utils.py:95-116` — `normalize_path` が `p.resolve()` を呼ばず symlink 未展開でプロジェクト境界判定（symlink バイパスの理論的余地、ローカルで実害ほぼなし）。

## Info（11件・修正不要）

- [I-1][PG] `post-tool-use.py:140` — `return None` 冗長（RET501）
- [I-2][SE] `card_generator.py:1297,1302` — `specs_dir.glob("*.md")` 非再帰（サブディレクトリ仕様書を拾わない）
- [I-3][SE] `scale_detector.py:72-76` — `_check_plan_a()` の reason 文字列が片方インストール時も両方 installed 表示
- [I-4][SE] `gitleaks_scanner.py:176-219` — `tempfile` 失敗時 `report_path` 未定義で finally NameError 経路
- [I-5][SE] `_hook_utils.py:38-48` — `LAM_PROJECT_ROOT` 不正パス時に警告なしフォールバック
- [I-6〜I-8] test-quality Info（マーカー混在・命名等、実害なし）
- [I-9〜I-11] arch/security Info（意図的設計・docs対象外等）

---

## 帰責判断が必要な Issue / PM級一覧

| # | Issue | 等級 | 分類 | 方針候補 |
|---|-------|------|------|---------|
| C-1 | 循環検出の戻り値契約 | PM | spec_ambiguity | ⑤分割時に戻り値型を `Optional`/例外へ。仕様明確化が先決 |
| W-5 | _SECRET_PATTERN 偽陽性 | PM | — | 検出パターン精緻化 or 対象拡張子限定（要仕様判断） |
| W-6 | _FR9_PATTERNS が hooks テスト DENY | PM | — | building 戻り時の例外許可（仕様齟齬の確認が先） |
| W-10 | check_g2/g5 未実装 | PM | spec_drift | 別 Wave スコープか確認。未実装なら docs/tasks 起票 |
| W-13 | SKILL.md 引数順コメント誤り | SE | spec_drift | 実装に合わせコメント修正（要実地確認） |

---

## Stage 4 検証結果（実地確認による訂正）

Active Retrieval により各 Issue を実コードで検証し、以下を訂正:

### 偽陽性・既知・意図的設計（除外）
- **W-5（偽陽性）**: `_SECRET_PATTERN` / `sec_fail` / `_run_security` は**現コードに存在しない**（リファクタで除去済み）。src-quality の指摘は古い agent-memory 由来のハルシネーション。security エージェントの「解消済み」が正しい。
- **W-6（意図的設計）**: `_FR9_PATTERNS` の `.claude/hooks/` deny は FR-9 D5「自律エンジンが強制機構自体を書換える再帰ハザード防止」（層1/層2 二重防御）。AUTONOMOUS active 時のみ発火し通常 BUILDING では hooks テスト修正可能。→ Info。
- **W-10（既知計画）**: `check_g2_lint.py`/`check_g5_security.py` は `docs/specs/autonomous-mode/tasks.md` **T2-1（SE・未着手）** として明示追跡済み。現 MVP が G1 のみは計画通り。spec_drift ではない。
- **W-3（前提誤り→Info）**: `pre-compact.py:96-98` の `main()` が全体を `try/except` 捕捉済み。OSError は捕捉される。残るのは partial-write の低確率リスクのみ。
- **I-4（誤検知）**: `with tempfile.NamedTemporaryFile`（176-177）は `try/finally`（181-219）と別ブロック。tempfile 失敗時は finally 未到達で NameError は起きない。

### 修正済み（PG/SE・本イテレーション）
- ✅ ruff F401 3件（`test_e2e_review.py:12,23` / `test_gitleaks_scanner.py:22`）— fixture `combined_issues.py` は意図的 Issue のため除外。
- ✅ W-2: `_save_loop_log` の握りつぶし → WARNING ログ化。
- ✅ W-11: `gitleaks_scanner.py:21` 相対 import → `from analyzers.base` に統一。
- ✅ W-13: SKILL.md の `merge_contracts` 引数順コメント修正（実装確認済み）。
- 検証: 両スイート **668 passed**・回帰なし。

### ⑤送り（card_generator 分割時・追跡: docs/tasks/card-generator-split-deferred.md）
- C-1 / C-2 / W-4 / I-2 — SESSION_STATE 調整2 に従い⑤（/building・TDD 強制）へ繰越。

### 残存 genuine SE Warning（6件・要判断）
- W-1 `main()` 50行超（Long Function）
- W-7 `DEFAULT_STATE` 2ファイル重複（既知・テスト保守性）
- W-8 `run_pipeline._detect_analyzers/_persist_results` 直接テストなし（既知・カバレッジ）
- W-9 tdd 通知B 未実装（genuine spec_drift・PG級・通知機能）
- W-14 `_run_g1_checker:269` env 全継承（CI 展開時のみリスク・ローカル無害）
- W-15 `normalize_path` symlink 未展開（理論的・ローカル無害）

## 真の Green State 評価

本ターゲットの Green State（Critical=0 ∧ Warning=0）は、**card_generator の Critical 2件が⑤に意図的繰越**のため本イテレーションでは到達不可。これは A-2 の技術的免除（⑤の TDD で対応する計画）に該当。残 6 SE Warning は非ブロッキング（Critical/セキュリティ/データ損失なし）。

## 修正方針（Stage 4 プラン・当初）

### 即修正可（PG/SE・低リスク・本イテレーションで対応）
- ruff F401 3件（fixture `combined_issues.py` は意図的 Issue のため除外）
- W-2（_save_loop_log の握りつぶし → ログ化）
- W-3（pre-compact OSError 捕捉）
- W-11（gitleaks_scanner の相対 import 統一）
- W-13（SKILL.md コメント・実地確認後）
- I-4（gitleaks report_path 未定義経路）

### ⑤送り（card_generator 分割時・TDD 強制下で対応）
- C-1 / C-2 / W-4 / I-2（card_generator 構造・戻り値契約系）— 調整2 に従い深追いせず⑤へ。

### PM級・ユーザー判断待ち
- W-5 / W-6 / W-10（仕様齟齬・仕様明確化が先決）

### 要検討（SE だがリスク/工数あり）
- W-1（main 分割）/ W-14（env allowlist）/ W-15（normalize_path resolve）/ W-7（DEFAULT_STATE 統合）/ W-8・W-9（テスト追加・通知B実装）
