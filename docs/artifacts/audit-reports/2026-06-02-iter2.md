# 監査レポート — full-review iter2

**対象**: `.claude/hooks/`（実装本体 + tests・15,870行）
**実施日**: 2026-06-02
**Scale**: Plan A のみ（B/C/D 無効・tree-sitter/import-map なし）
**前提**: iter1 修正後のゼロベース再監査。card_generator の C-1/C-2/W-4/I-2 は⑤(BUILDING)へ A-2 免除・繰越継続。

## サマリー

| 重要度 | 新規 | 繰越/追跡済み | 偽陽性・棄却 |
|--------|------|--------------|--------------|
| Critical | **0** | 2（C-1/C-2＝⑤） | — |
| Warning | 8（修正対象 SE 6 / PM 2） | W-9, W-12, W-14, W-15 | 1（pytest markers） |
| Info | 多数（PG/任意） | — | — |

**総合評価: B**（新規 Critical ゼロ。A-2 免除の前提＝card_generator のみ繰越を維持。残 Warning は SE/PM）

> 真の Green は⑤後（card_generator の Critical 2件解消後の再監査で確定）。iter2 も iter1 同様 deferred 収束。

## 静的解析（Stage 1）

| ツール | 結果 |
|--------|------|
| ruff | F401×1（`fixtures/e2e/combined_issues.py`・意図的サンプル・対象外）→ 実質0 |
| bandit | B314×1（`post-tool-use.py:90` XML→SEC-N1）/ B108×2（test・Info） |
| gitleaks | 0件（no leaks・G5 PASS） |

## 新規 Warning（修正対象 = SE 級）

| # | 等級 | 箇所 | 内容 |
|---|------|------|------|
| W2-3 | SE★ | `pre-tool-use.py:187-189` | `_read_current_phase()` の `except Exception: pass`（ログなし握りつぶし）。AUTONOMOUS の FR-9/FR-3.4 判定に直結＝フェイルオープン方向。iter1 W-2 と同型 → `sys.stderr` ログ追加 |
| W2-1 | SE | `lam-stop-hook.py:282-332` | `_handle_autonomous()` 51行（50行超）。G1呼出+分岐+状態書込の3責務 |
| W2-2 | SE | `pre-tool-use.py:111-170` | `_determine_level_and_reason()` 60行（権限判定中核・認知負荷高） |
| W2-5 | SE | `pre-tool-use.py:250-257` | DENY 出力に `hookEventName` 有・ask 出力に無し（非対称） |
| W2-6 | SE | `tests/test_stop_hook.py:17` ほか | `DEFAULT_STATE` が2ファイル重複定義 → conftest 集約 |
| W2-7 | SE | `tests/test_autonomous_state.py` | `state_file_path()` 戻り値の `isinstance(Path)` アサート欠落 |
| W2-8 | SE | `tests/test_loop_integration.py` | `test_init_fail_then_converge` 50行3フェーズ詰込（block 検証重複） |

## 新規 Warning（PM 級 = 指摘のみ・追跡）

| # | 等級 | 箇所 | 内容 |
|---|------|------|------|
| W2-P1 | PM | `javascript_analyzer.py:53-116` | ESLint flat config（`eslint.config.*`）未検出。ESLint v9 普及で実害率増。検出ロジック追加は仕様判断 |
| W2-P2 | PM | `SKILL.md` Stage1 Step3 / `run_pipeline.py` | **W-6**: SKILL.md は「import-map.json を run_phase0 が永続化」と記すが実装に生成処理なし（`save_import_map` 不在）。Plan D を無効化。現状 Plan A のみで実害なし |

## Info（PG/任意・Green 阻害せず）

- `post-tool-use.py:140` `return None` 冗長（ruff RET501）[PG]
- SEC-N2 `test_e2e_review.py` env 全継承 [PG]／SEC-N3 `target` の Unicode 双方向制御文字フィルタ [PG]
- `gitleaks_scanner.py:176-219` `report_path` 未定義時 NameError 可能性 [SE/Info]
- `_hook_utils.py:43-44` `LAM_PROJECT_ROOT` 不正時ログなしフォールバック [SE/Info]
- `card_generator.py:952` `_condense_sccs()` O(N^2)（⑤分割時に改善）[SE]

## 繰越・追跡済み（再カウント外）

- **⑤送り（`card-generator-split-deferred.md`）**: C-1, C-2, W-4, I-2 ＋ 新規 W2-4（`_bfs_upstream` の `queue.pop(0)`→deque, `card_generator.py:1143`）・`_condense_sccs` O(N^2) も分割時に一括解消が効率的
- **W-9**（通知B未実装・`tdd-notification-b-deferred.md`）
- **W-14/W-15**（env継承・symlink・`hooks-security-hardening-ci.md`）
- **W-12**（state_dir パス不整合・本 iter2 でも `.claude/hooks/.claude/` junk を再生成）

## 偽陽性・棄却

- test-quality の「pytest マーカー未登録（PM）」→ `pyproject.toml` に `e2e_llm`/`e2e_convergence` **登録済み**（addopts で使用中）。棄却。
- SEC-N1（B314 XXE）→ Python 3.8+ デフォルト安全 + 信頼境界内 XML。`[Low][PG]`・対処不要。

## 構造整合性（良好）

settings.json ⇔ hook スクリプト全一致 / hook チェーン断絶なし / lam-loop-state・autonomous-state スキーマ一致 / 循環依存なし。（import-map のみ書き手不在＝W2-P2）
