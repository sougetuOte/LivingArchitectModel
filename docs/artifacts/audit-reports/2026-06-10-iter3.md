# 監査統合レポート（B-2 full-review イテレーション3）

**実施日**: 2026-06-10
**対象**: `.claude/hooks/`（ゼロベース再スキャン・3巡目）
**構成**: 監査6体（SRC-A/SRC-B/TEST-A/TEST-B/SEC/QA・sonnet）+ 監督査定（本体）
**Stage 1**: 静的解析 Issues 0 / gitleaks クリーン

## サマリ

```
生指摘: 約30件（SRC-A 6 / SRC-B 5 / TEST-A 3 / TEST-B 5 / SEC 5 / QA 8 ※確認済み除く）
監督査定: 棄却 4 / Warning→Info 降格 8 / High→Info 降格 1
確定: Critical 0 / Warning 5（全件修正済み）/ PM級 3（仕様補記・承認待ち）/ Info 多数
修正: Warning 5件 + Info 自明修正 4件（計9）
検証: 738 passed（735 + 新規3・回帰ゼロ）/ ruff clean
```

## 確定 Warning（全件修正済み）

| # | 等級 | 箇所 | 内容 | 修正 |
|---|------|------|------|------|
| W3-1 | SE | `pre-tool-use.py:230` | `main()` 69行（Long Function。iter1 で lam-stop-hook main を同基準で修正した先例と整合） | `_build_deny_reason()` / `_emit_permission_response()` を抽出。main は判定とログに専念 |
| W3-2 | SE | `card_generator.py:940` | `collect_spec_drift_context` の仕様書 read_text に OSError 防御なし（同関数内のカード読込と非対称。カード読込側も OSError 未捕捉だったため両方修正） | try/except + warning + continue。TDD テスト追加（*.md 名ディレクトリで再現） |
| W3-3 | SE | `javascript_analyzer.py:190` / `rust_analyzer.py:215` | `parse_ast` が `errors="replace"` なしで非 UTF-8 ファイルにより UnicodeDecodeError 停止（PythonAnalyzer と非対称） | `errors="replace"` 付加。TDD テスト2件追加 |
| W3-4 | SE | `test_loop_integration.py:52,102,146,235` | `_create_makefile` セットアップが Dead Code（lam-stop-hook は Makefile を読まないことを grep で監督確認）。テスト名 `test_test_failure_blocks` も実態と乖離 | 関数 + 呼出3箇所を削除。テスト名を実態（安全ネット block）に改名 |
| W3-5 | SE | `test_tdd_notification_b.py:58` | テスト名が W-22 遷移条件を隠蔽（iter2 で docstring 対応したが名前自体が誤解を招く） | `test_no_analyzed_marker_with_transition_counts_all_entries` にリネーム |

## Info 降格・自明修正4件実施

- SEC-1（High→Info 降格）: `_run_gitleaks` に `shell=False` 明示。**降格理由**: Python デフォルトが shell=False で現状安全・「将来のコピペミス」は投機的。High/PM は過大。防御的明示として実施
- SRC-B-4 [PG]: `_run_gitleaks` の `cmd` in-place 変異を `[*cmd, ...]` の新リスト生成に変更
- SEC-5 [PG]: G1 checker stderr のログ記録時に改行・タブを除去（ログ行構造の保全）
- QA-10 [SE]: `lam-orchestrate/SKILL.md` の旧 Phase 表記8箇所を full-review の Stage 体系に更新（Phase 1→Stage 0 等。lam-orchestrate 自身の Phase 体系とは区別）

## 棄却（監督査定）

| 生指摘 | 棄却理由 |
|--------|---------|
| SRC-A3-W-5（`_read_current_phase` 二重読取） | iter1 で SRC-A-3 として **Info 記録のみ** と査定済みの同件再出 |
| TEST-B-1（`_make_mock_result` 重複3実装） | iter1 TEST-B-5 で Info 先例。guideline の閾値は「Duplication **> 3回**」であり3実装は閾値内 |
| TEST-B-2（`_patch_find_sccs_to_raise` の偽 Pass リスク） | monkeypatch の `raising=True` デフォルトが属性消失時に AttributeError で**ラウドに失敗する**ため「無音の偽 Pass」主張は誤り。新規利用箇所の追加漏れリスクのみ残存（docstring 記録済み）→ Info |
| QA-8（`hook_event_name` の存在未確認） | **context7 で公式ドキュメント裏取り完了**: `hook_event_name` は全 hook イベント共通入力フィールドであり、PostToolUseFailure の入力例にも明示。実装は正しい |

## Warning→Info 降格（記録のみ）

- SRC-A3-W-2: `normalize_path` 54行 — docstring/W-15/16 セキュリティ根拠コメントが大半で実質ロジック ~25行。コメントは削るべきでない
- SRC-A3-W-3/W-4: `_handle_test_result` 7引数 / `_record_fail` 7引数 — 単一呼出経路の private helper 連鎖。dataclass 導入は単一使用箇所の抽象化でありアンチパターン「過剰な抽象化要求」に近い。将来 post-tool-use に責務追加する際の再検討事項として記録
- SRC-B-3: `scale_detector._persist_result` OSError 未捕捉 — `__main__`/CLI 専用経路であり fail-loud（非ゼロ exit でStage 0 が可視的に失敗）が正しい挙動。warning 化すると exit 0 で出力なしの方が有害
- SEC-2: `_write_json_file`/`_write_card_file` 非アトミック — review-state は再生成可能なキャッシュ成果物・読み手は JSONDecodeError フォールバック実装済み。loop/autonomous 状態（不可逆）とのアトミック性非対称は意図的設計と査定
- SEC-3: `review-config.json` 数値検証なし — 「攻撃者が config を書ける」前提はリポジトリ書込可能=既に統治外であり PM-3 議論と同じ脅威モデル外。設定 typo 起因の縮退は gitleaks 経路が Critical Issue でラウドに検出される
- SEC-4: `backup_loop_state` の `shutil.copy2` 非アトミック — `.bak` は参照専用（hook が読まない）
- SRC-A3-I-1 / SRC-B-5 / TEST-B-3/4/5 / QA-2 / QA-5: 各エージェント報告どおり Info 記録

## PM級 — 承認待ち（3件・いずれも仕様書への追記）

| # | 箇所 | 内容 | 提案 |
|---|------|------|------|
| PM3-1 | `tdd-introspection-v2.md` §4.1 | tdd-patterns.log の第5フィールド（失敗テスト名要約）の PASS 時の値が仕様未定義。実装は `"{test_cmd} (previously failed)"` を記録（機能影響なし: 読み手は第2フィールドのみ参照） | PASS 時フォーマットを仕様に補記 |
| PM3-2 | `green-state-definition.md` §6 付近 or `autonomous-mode` 仕様 | AUTONOMOUS 完了判定（G1 のみ・T2-1 で G2/G5 は Wave 2 予定）と green-state-definition の G5「MVP ○」の境界が読み手に見えない（QA-3/QA-4 同根。iter2 D-1 の精密化） | AUTONOMOUS モードの G5 扱い（Wave 2 まで G1 のみ）を注記 |
| PM3-3 | `gitleaks-integration-spec.md` NFR-1 | 性能目標「gitleaks detect 30秒以内」に対し実装 `timeout=120`。実装は大規模リポジトリ対応で意図的に余裕を持たせている | NFR-1 に「30秒は性能目標値・実装のハードタイムアウトは 120s」と補記 |

## 次アクション

1. PM3-1〜3 をユーザーに提示（pm_pending=true・ループ一時停止）
2. PM 修正後、イテレーション4: Stage 2 ゼロベース再スキャン（Before=0 確認）
