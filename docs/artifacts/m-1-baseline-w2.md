# M-1 ベースライン測定（W2 末）

## メタ情報

| 項目 | 内容 |
|:-----|:-----|
| Milestone | M-1 / Wave 2（規律本体の適用）末 |
| 測定 Task | W2-M1-T9（W2 末測定 + 件数突合） |
| 測定日 | 2026-07-26 |
| 手順 | `tasks.md` §9 Wave 末測定の共通手順（W0 / W1 と同一 = 比較可能性の担保） |
| 比較対象 | `docs/artifacts/m-1-baseline-w0.md`（起点）/ `m-1-baseline-w1.md`（前 Wave） |
| 対応仕様 | requirements.md FR-7, FR-8 / NFR-3 / NFR-6 |

> **新規機構を追加していない**（NFR-2）: 測定に新規スクリプト・新規ログを作成していない。項目 6 の文字数は W0 と同一のコマンドで測定した。

---

## 1. 測定 6 項目

| # | 項目 | W0 起点 | W1 末 | **W2 末** | 差分（W1 比） |
|:-:|:-----|:--------|:------|:----------|:--------------|
| 1 | pytest 全数 | 1047 + 14 | 1047 + 14 | **1085 passed + 14 skipped**（exit 0） | **+38**（W2-M1-T4 の新規テスト / **regression ゼロ**） |
| 2 | Green State 件数 | C0 / W0 | C0 / W0 | **C0 / W0**（下記 1.2） | ±0 |
| 3 | `tdd-patterns.log` FAIL→PASS 率 | 算定せず | 算定不能（母数 0） | **算定不能（母数 0）**（下記 1.3） | — |
| 4 | gabriel verdict 分布 | refuted 3（母数 3） | W1 中 probe 0 件 | **W2 中 probe 0 件**（log 最終更新 2026-07-18 のまま） | 増加なし |
| 5 | PM 級ダイアログ発火数 | 0 | 0 | **7 パス**（下記 1.5） | +7 |
| 6 | `CLAUDE.md` + rules 文字数 | 154,837 字 / 2,749 行 | 154,837 / 2,749 | **169,030 字 / 2,905 行** | **+14,193 字 / +156 行**（下記 1.6 = **要注意**） |

### 1.1 項目 1（pytest）

```bash
bash .claude/scripts/py_invoke.sh -m pytest
```

`1085 passed, 14 skipped` / exit 0。**PASS 数の減少なし・FAIL 0 件**（NFR-3 充足）。増加 38 件は W2-M1-T4 で新設した `.claude/tests/scripts/test_verify_model_reference.py`。

### 1.2 項目 2（Green State 件数）

W2 では AUDITING を実施していないため、共通手順の定義に従い直近ゲート記録の値（Critical 0 / Warning 0）を転記。W2 は規範文書の編集が主体であり、Issue が発生する経路は W2-M1-T4 の新規スクリプトのみ。同スクリプトは 38 テストで Green。

### 1.3 項目 3（`tdd-patterns.log` FAIL→PASS 率）

- ログ総行数 **249**、最終 `ANALYZED` マーカーは 249 行目（2026-07-25 / W1 retro 記録）
- 以降の新規エントリは **0 行** = W2 中の FAIL→PASS 遷移は発生していない
- W2-M1-T4 の TDD は Red（collection error）→ Green を経ているが、**hook は「直前に FAIL があった場合の PASS」を遷移として記録する**ところ、Red は subagent 側の実行かつ collection error（テスト FAIL ではない）であったため遷移条件を満たさなかった
- したがって母数 0 で **算定不能**

### 1.4 項目 4（gabriel verdict 分布）

`.claude/gabriel-metrics.log` の最終更新は **2026-07-18**（W2 着手前）。W2 中の probe 実行は **0 件**。W2 は MAGI 合議を要する判断（AoT 適用条件）を含まず、判定は W1 で確定済みの写像作業であったため、probe が発火する経路がなかった。

### 1.5 項目 5（PM 級ダイアログ発火数）

`.claude/.session-pm-edit-cache.json` に記録された**承認済み PM 級パスは 7 件**:

`model-roster.md` / `model-delegation-prompting.md` / `hga-summoning.md` / `phase-rules.md` / `fable-l3-protocol.md` / `terminology.md` / `planning-quality-guideline.md`

- 承認形式は **K5 一括宣言 1 回**（W2 冒頭にファイル一覧を提示し、ユーザーが「T2 に進んで」で着手を承認）。以後は `permission-levels.md` §セッションスコープ降格により同一ファイルの 2 回目以降が SE 級扱い
- `CLAUDE.md` は PM 級パス列挙に含まれないため本カウント外（`permission-levels.md` §ファイルパスベースの分類）
- **W1 の 0 件との差は仕様どおり**（W1 は判定のみ / W2 が適用 Wave）

### 1.6 項目 6（文字数）— **増加している（要注意）**

```bash
cat CLAUDE.md .claude/rules/*.md .claude/rules/*/*.md | wc -m   # 169030
cat CLAUDE.md .claude/rules/*.md .claude/rules/*/*.md | wc -l   # 2905
```

**W1 末比で +14,193 字 / +156 行と増加した。** 内訳:

| 要因 | 字数 | 行数 |
|:-----|-----:|-----:|
| `model-roster.md` **新設**（W2-M1-T1 / SSOT の器） | **+17,020** | **+218** |
| 上記を除く規律本体の増減（T5 削減 42 件 + T6 ゲート改訂 + T7 追加 5 件） | **-2,827** | **-62** |
| 合計 | +14,193 | +156 |

**解釈（NFR-6 準拠 / 件数・行数は完了条件ではない）**:

- **条項トリアージの適用（T5）自体は純減**である。roster を除いた規律本体は **152,010 字 / 2,687 行**（W1 末比 **-2,827 字 / -62 行**）
- 増加の実体は **SSOT の器を新設したこと**であり、その中身の大半は「これまで各所に散っていた記述の移設（T2 / T3）」と「W0-M1-T5 で裏取りした一次情報の転記」である。**同一情報の重複を減らした一方で、総量は増えた**
- `CLAUDE.md` は **269 行 → 260 行**（-9 行 / 公式推奨 200 行以下には未到達）
- **この増加を W2 の失敗とは判定しない**（NFR-6 = 削減率・削減行数を完了条件に置かない）。ただし M-1 の趣旨（規律肥大化の解消）に対する**逆行の可能性**として W4-M1-T5 retro の議題に送る。特に「roster が常時ロードされる 218 行として妥当か（`paths:` 条件ロードの対象にすべきか）」は未判定

---

## 2. 件数突合（FR-7 受け入れ条件 3 / W2-M1-T9）

### 2.1 突合結果: **一致**

| 対象 | W1 トリアージ表 | 削減台帳（主表） | 判定 |
|:-----|:----------------|:-----------------|:-----|
| 圧縮 | 13 | 13 | ✅ 一致 |
| 削減 | 27 | 27 | ✅ 一致 |
| **小計（FR-7 が突合を要求する範囲）** | **40** | **40** | ✅ **一致** |

### 2.2 台帳の全体構成（51 行 + 付表 5 行）

| 区分 | 件数 | 内訳 |
|:-----|-----:|:-----|
| W1 トリアージ由来 | **43** | 圧縮 13 / 削減 27 / 運用移管 2 / SSOT 退避 1（`CLAUDE.md#Execution-Modes-01`） |
| 設計由来（`-m` 系 / W1 トリアージ外） | **8** | SSOT 退避 6（T2 の 4 + CLAUDE.md 作業体制・モデル運用）/ 圧縮 2（担当層の model ID・AutoMode classifier） |
| **主表 計** | **51** | — |
| 付表（§B 未執筆条項の追加 / T7） | 5 | 圧縮 2 / 保全 3 |

**差分の説明**（不一致ではない）: 台帳には FR-7 が突合対象とする「圧縮 + 削減」以外に、**運用移管 2・SSOT 退避 1・設計由来 8** が載る。設計由来 8 件は W1 の 112 条項に含まれない（design §6.1 の移行手順および FR-11 に由来する）ため、トリアージ表側に対応行が存在しない。台帳側で `-m` サフィックスにより識別可能。

### 2.3 保全・対象外の無変更確認

`git diff 3412467..HEAD -- CLAUDE.md .claude/rules/` により、**保全 57 件・対象外 12 件の条文に変更が加えられていないこと**を確認した（変更箇所はすべて台帳記載の条項、または T2 / T3 の SSOT 退避、または T6 / T7 の追加）。

---

## 3. W2 の完了状況

| Task | 状態 | 備考 |
|:-----|:-----|:-----|
| W2-M1-T1 | ✅ 完了 | `model-roster.md` 新設（4 項目 + ADR-0001 直交性 + L2/L3 への Opus 非割当） |
| W2-M1-T2 | ✅ 完了 | SSOT 退避 2 ファイル（挙動デルタ → §3 / 単価・envelope → §4） |
| W2-M1-T3 | ⚠️ **部分完了** | モデル ID 除去・導線・drift ゼロは達成。**`/doctor` trim 提案の取得が未実施**（下記 §4） |
| W2-M1-T4 | ✅ 完了 | `verify_model_reference.py` + 38 tests / 実走査 total_drifts=43 |
| W2-M1-T5 | ⚠️ **部分完了** | 42 件適用 + 台帳記入は完了。**`paths:` frontmatter 付与 5 件が未実施**（下記 §4） |
| W2-M1-T6 | ✅ 完了 | 召喚ゲート改訂（新ゲート + 移行期規定） |
| W2-M1-T7 | ⚠️ **部分完了** | §B 6 件中 5 件反映。**NFR-W-C-1 数値改訂 1 件が未実施**（下記 §4） |
| W2-M1-T8 | ✅ 完了 | 復活経路を台帳の運用記述として接続（新規機構ゼロ） |
| W2-M1-T9 | ✅ 完了 | 本測定 + 件数突合（一致） |

---

## 4. 未完了 3 件（いずれもユーザー判断が必要 / 自律実行では閉じられない）

| # | 未完了項目 | 理由 | 必要な判断 |
|:-:|:-----------|:-----|:-----------|
| 1 | **`/doctor` trim 提案の取得**（T3） | `/doctor` は対話型ターミナルパネルを開くコマンドであり、本セッション（非対話）からは実行できない | ユーザーが対話セッションで `/doctor` を実行し、`CLAUDE.md` への trim 提案を取得する。採否は条項単位の L1 判断（W1 で「保全」判定済みの条項を `/doctor` の提案のみを根拠に削ってはならない） |
| 2 | **`paths:` frontmatter の付与 5 件**（T5） | **機構の粒度ギャップ**: `paths:` は**ファイル単位**で作用するが、W1 の「ロード条件 = 条件」判定は**条項単位**で下されている。`CLAUDE.md`（4 件）に付与すると CLAUDE.md 全体が条件ロード化し、`terminology.md`（1 件）に付与すると同ファイル内の**無条件ロード判定を受けた保全条項まで**条件ロード化してしまう | 「該当条項を独立した rules ファイルへ切り出して `paths:` を付ける」か「条件ロードを見送る」かの構造判断（**PM 級**）。design §5.2.4 は「属性は frontmatter の除去だけで巻き戻せる」としており、**見送っても不可逆な損失はない** |
| 3 | **NFR-W-C-1 の数値改訂**（T7 / gabriel タイムアウト 60 秒 SHOULD → 実測 294 秒ベース） | 改訂対象の数値 SSOT は `docs/specs/magi-v2-gabriel/requirements.md` §NFR-W-C-1 にあり、**W2 の K5 一括宣言に含まれない PM 級ファイル** | 追加の K5 宣言 + 承認（FR-3 受け入れ条件 3） |

---

## 5. 参照

- `docs/specs/m-1-opus5-migration/tasks.md` §9（測定の共通手順）/ §6（W2 Task 定義）
- `docs/artifacts/m-1-baseline-w0.md` / `m-1-baseline-w1.md`（比較対象）
- `docs/artifacts/m-1-clause-ledger.md`（削減台帳 / 突合の相手方）
- `docs/artifacts/m-1-triage-table.md` §G.5（W2 への引き渡し 43 件）
- `docs/artifacts/m-1-opus5-stability-gate.md`（W1 → W2 ゲート判定）
