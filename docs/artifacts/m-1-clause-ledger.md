# M-1 削減台帳

| 項目 | 内容 |
|:-----|:-----|
| Milestone | M-1（Opus 5 移行 + 規律の条項トリアージ） |
| 対応 Task | W0-M1-T4（本スケルトン作成）/ W2-M1-T5（データ行の記入）/ W2-M1-T9（件数突合） |
| 対応仕様 | `docs/specs/m-1-opus5-migration/design.md` §6.3 / requirements.md FR-7 |
| 作成日 | 2026-07-25 |
| ステータス | **スケルトン**（W0 時点でデータ行ゼロ / 記入は W2） |

---

## 帳簿単一原則（NFR-2 受け入れ条件 1 / MUST）

> **本台帳は記録表であり、成果物の合否を判定する帳簿ではない**（帳簿単一原則 / 成果物判定は Green State 1 冊のみ）。

`fable-l3-protocol.md` §3 が定めるとおり、成果物判定の帳簿は **Green State（Critical / Warning 件数）1 冊のみ**である。本台帳への記載件数や記載の有無が Green State の判定条件（G1〜G5）に加算されることはない。また記載件数を削減目標として用いない（NFR-6）。

---

## 記録対象

**W2 で実ファイルに変更を加えた全条項**を記録する。

- 判定が「保全」でも `paths:` frontmatter を付与した場合は**記録対象に含める**（変更を加えたため）
- 判定が「保全」かつロード条件も無変更の条項は**記録しない**
- 「対象外」（不変制約 4 対象）も**記録しない**

> 決定木の出力は 6 値（design §5.2）だが、本台帳に載るのは実ファイルを変更した 5 パターンである。「保全（無変更）」と「対象外」はトリアージ表（`docs/artifacts/m-1-triage-table.md`）側で追跡する。**したがってトリアージ表と本台帳の件数は一致しない**（W2-M1-T9 の突合時に織り込むこと）。

---

## 台帳（8 列 / 閉集合）

| 条項ID | 原文 | 出典（ファイル:節） | 判定軸（軸1〜4 + 基質適合） | 判定 | 移動先 / 監視条件 | ロード条件 | 判定日 |
|:------|:-----|:-------------------|:---------------------------|:-----|:------------------|:-----------|:-------|

*(W0 時点でデータ行はゼロ。記入は W2-M1-T5 で行う)*

---

## 列の定義

### 「判定」列（5 値の閉集合）

| 値 | 意味 |
|:---|:-----|
| 圧縮 | 意図 1 行に畳んだ（根拠は `docs/artifacts/` へ退避） |
| 削減 | 条文を削除した（**懸念ごと消す** = リスクが実在しないと判定） |
| SSOT 退避 | 別 SSOT へ移動した（削除ではない） |
| **運用移管** | 機構（hook / test / rule 条文）での担保をやめ、**運用（retro 監視）へ移した**。条文は消えるが**リスク受容の記録は残る** |
| 保全（ロード条件変更のみ） | 条文は変えず `paths:` frontmatter のみ付与した |

### 「移動先 / 監視条件」列の書式

| 判定 | 記入内容 |
|:-----|:---------|
| SSOT 退避 | 移動先のファイル:節 |
| 圧縮 | 根拠の退避先（`docs/artifacts/...`） |
| **運用移管** | **`retro / シグナル: <1 行> / 失効: YYYY-MM-DD`** |
| 削減 | `—` |
| 保全（ロード条件変更のみ） | `—` |

> **運用移管の 3 フィールドを独立列に展開せず 1 セルに畳む理由**: 列の増加が新しい帳簿の胚になることを避けるため（NFR-2 / design §6.3）。

### 「ロード条件」列の書式

`無条件` / `条件: <glob>` のいずれか。判定が「削減」「運用移管」の場合は `—`。

---

## 「運用移管」を選択する要件（design §5.2 / MUST）

「削減」との違いは、**リスクの存在を認めたうえで機構化しないと決めた**点にある。リスクを機構なしで受容する決定は**それ自体が軸 1 のユーザー意思（リスク許容度の宣言）**であり、無記録で消してはならない。

**判定 3 条件（すべて充足する場合のみ選択可）**:

1. **発火が無計装でも喧しい**（普通に作業していれば気づく）— **主ゲート**。機構の存在理由は沈黙する失敗の検出であるため、失敗が自ら騒ぐなら機構は冗長。逆に**静かに潜伏する類（データの静かな破損 / セキュリティ）は運用移管を永久に不可**とする
2. **Phase A を通過済み**（可逆・爆風半径が有界）
3. **発火頻度 × 復旧コスト < 機構維持コスト**（機構が**将来の監査対象面になる**二次コストを算入する）

**同時保有上限**: **5 件**（監視面が retro 1 回分の議題容量に制約されるため / W1-M1-T6 で再確認）。

**承認**: 運用移管の判定は**ユーザーの PM 級承認**を要する（軸 1 の判断であるため / W1-M1-T6 の一括承認に含める）。

**再機構化の経路（新機構ゼロ）**: 移管済みリスクが発火した場合、それは `trust-model.md` §カウント単位が定める「**検出イベント**」1 カウントであり、閾値 2 で既存の draft rule 起票フローに**自動合流**する。独自のエスカレーション機構は作らない。

**失効日の意味（対称）**: 窓内に発火すれば既存機構が拾い、沈黙のまま満了すれば懸念ごと消滅する（リスクが実在しなかった事後証明）。**どちらに転んでも閉じる**。

---

## 既存 3 概念との区別（意味論 / design §5.2）

| 概念 | 意味論 | 帰結 |
|:-----|:-------|:-----|
| **deferred**（`r-1-audit-tracker.md` の裁定） | **未決定** | tracker に未完了作業として居座り、以後の design doc に再出現する（R1-054〜058 が実証）。**在庫自体がループの燃料** |
| **characterization pin**（rule-002 の機構実体） | **観測の機構化・判断の先送り** | 判断していないのに保守コスト（same-commit 更新義務という恒常税）が発生し、監査対象面でもあり続ける |
| **運用移管**（本台帳） | **決定済み・非機構化を選択・クローズ** | 作業台帳から消え、リスク受容記録として残る |

---

## 参照

- `docs/specs/m-1-opus5-migration/design.md` §5.2（決定木 6 値 / 運用移管の要件 / 立証責任の所在）・§5.2.4（ロード条件属性）・§6.3（本台帳の形式 SSOT）
- `docs/specs/m-1-opus5-migration/requirements.md` FR-7（台帳）/ NFR-2（帳簿単一原則）/ NFR-6（削減率を目標にしない）
- `docs/specs/m-1-opus5-migration/tasks.md` W0-M1-T4（本ファイルの作成）/ W2-M1-T5（記入）/ W2-M1-T9（突合）
- `docs/artifacts/m-1-triage-table.md`（W1 のトリアージ表 / 「保全（無変更）」「対象外」はこちらで追跡）
- `docs/artifacts/hga-summon-log.md` #17（本台帳の形式を 7 列 → 8 列に改訂した判定の記録）
- `.claude/rules/auto-generated/trust-model.md` §カウント単位（運用移管の再機構化経路が接続する既存閾値）
