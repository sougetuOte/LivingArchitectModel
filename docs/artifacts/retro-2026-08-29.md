# Retro — セッション 27（2026-08-29）

**対象**: 2026-08-29 の 1 セッション（`/quick-load` 〜 `/retro`）
**フェーズ**: BUILDING / Milestone なし
**持ち越し議題 3 件**: (i) 判定者=当人 (ii) 安い通貨の抜け道 (iii) 先頭固定の効果測定
**前回**: `retro-2026-08-17.md`（12 日前 / この間 `/retro` は未実行）

---

## Step 1: スコープ

| | |
|:---|:---|
| 変更 | **21 ファイル変更 + 新規 2**（264 insertions / 113 deletions） |
| コミット | **0**（`/ship` 未実行 / 本 retro の後） |
| テスト | **1248 → 1277 passed**（14 skipped / 全工程で緑を維持） |
| 処理したタスク | #4 permission-levels / #5 flaky 調査 / #6 T5 の検査 / #7 安全確認 / S-3 配布物 / #8 機構化 |

---

## Step 2: 定量

| 指標 | 値 |
|:-----|:---|
| 配布物の修正箇所 | **51**（実在しないコマンド 42 + 独立レビュー指摘 9） |
| 対象ファイル | 日本語 4 / 英語 4 / スライド 6 / docs/internal 1 |
| テスト追加 | **+29**（R3 機構 #10） |
| 独立レビューの指摘 | **Critical 2 / Warning 3 / Info 12**（Critical は 2 件とも事実 → 修正済） |
| 修正後 | **Critical 0 / Warning 0**（WARN-2・WARN-3 は設計上の留意点として受容） |
| 新規機構 | **R3 #10**（command 検査 + directory 検査） |
| 通貨（誕生ゲート） | **発行 0 / 消費 0**（§A = 56 のまま不変） |

---

## Step 2.5: TDD パターン分析

**最終 `ANALYZED` = 2026-08-17（line 261）。以降 28 エントリ。**

### 頻出パターン（検出イベント単位）: **0 件** / ルール候補: **0 件**

| テスト名 | 生 FAIL 数 | 検出イベント | 判定 |
|:---|:---:|:---:|:---|
| `test_silent_when_no_log_file` | **2** | **1** | **本セッションの #5 調査由来のアーティファクト**（1 回の flaky 調査内 = 同一検証イベント） |
| `test_unpaid_debt_blocks_new_entry` | 2 | 1 | 同一デバッグループ（30 秒間隔） |
| その他（parser / dashboard / 機構 #8 / 機構 #10 開発） | 各 1 | 各 1 | 通常の Red→Green |

### 重要 —— **汚染は起票を生まなかった。既存のカウント単位が既にこの型を防いでいた**

本セッションの #5 調査は `.claude/tdd-patterns.log` に **FAIL 2 件 + PASS 1 件**を残した（append-only / 既存行の欠損なし / SHA 前後比較で確認済）。生の回数だけを見れば同一テスト名が 2 回で閾値に達するが、`trust-model.md` §カウント単位は「**1 つの検証イベント内で検出された複数 issue は件数によらず 1 カウント**」と定めており、2 件は 1 回の調査内であるため **1 カウント**にとどまる。

**つまり計器を汚したにもかかわらず、誤った起票は生じなかった。** これは幸運ではなく、2026-07 に定義したカウント単位が意図どおり働いた結果である。ただし**汚染の検出は事後**であり（実験前の SHA スナップショットで気づいた）、事前に防げてはいない。

---

## Step 3: KPT

### Keep

- **独立コンテキストのレビューが実際に効いた**。配布物の掃きを終えた直後に著者（L1）とは別コンテキストで監査を掛け、**Critical 2 件**を検出した。同じ時点で L1 の自己チェックが検出したのは **0 件**である
- **導出型検査（維持リストを持たない）が手作業の取りこぼしを捕まえた**。42 箇所を手で潰したあと機構を走らせたところ、`CHEATSHEET_en.md` の Skills 表に残っていた `/clarify` を検出した
- **陰性対照つきで機構を作る作法**。機構 #10 は開発中に自分のテストで実装バグを 2 件検出した（例外理由の長さ / `.claude/commands/` が自分の否定後読みに引っかかる正規表現）
- **判断を求めるときの提示形式**（根本・経緯・選択肢の詳細を先に出す / ユーザー常設指示）。逆質問の往復が発生しなかった
- **計器に触る前に SHA を記録する**（`security-commands.md` §計器への書き込みを伴う検証）。#5 調査の汚染はこれで検出できた

### Problem

- **P1: 自分で書いたチェックリスト項目を、同じコミットの中で自分が破った。** `/release` Phase 2.5 に「**日本語版だけ直して英語版を置き去りにしていないか**」を新設したそのコミットで、空の `.claude/commands/` の記載を 7 箇所（CHEATSHEET 日英 / QUICKSTART_en / docs/internal ×2 / スライド日英）残した。**人手チェックリストは、それを書いた本人に対しては機能しない**
- **P2: 60 秒実況の発火点が 2 つの正本で食い違っている。** `fable-l3-protocol.md` §5.1 は「**PLANNING** 承認要求提出直前」と修飾するが、`CLAUDE.md:195` は「承認要求提出直前」と**修飾を落としている**。本セッションは BUILDING で承認要求を 5 回以上提出しており、**どちらを読むかで実況の義務が「0 回」と「5 回以上」に分かれる**。しかも**緩い方（修飾なし = 広い義務）が、唯一無条件ロードされる `CLAUDE.md` に載っている**
- **P3: 計器の汚染は防げず、事後検出だった。** 閾値には届かなかったが、`.claude/tdd-patterns.log` に調査由来の FAIL が入った事実は変わらない
- **P4: T5 が 5 回持ち越された。実行の契機は先頭固定ではなかった。** 今日実行できたのは、セッション冒頭のプランに**独立タスクとして明示的に載せた**からである（Phase 1 の並列枠）
- **P5: 大きめの Python を bash heredoc に埋めるとシェルに潰される。** `README_en.md` 生成時に `unexpected EOF while looking for matching '` で失敗した。Write ツールでスクリプトを作ってから実行する形に切り替えて解決

### Try

- **T1: 実況発火点の食い違いを解消する。** `CLAUDE.md:195` に `PLANNING` 修飾を戻すか、protocol §5.1 を広げるか。**どちらが正かは PM 級判断**であり、本 retro では決めない（両ファイルとも PM 級）
- **T2: 「作業の締めに独立レビュー」を運用として続ける。** 今回 n=1 で Critical 2 件。条項化はしない（既にユーザー常設指示がある）
- **T3: 埋め込み Python は heredoc を使わず Write ツール経由にする**（P5 の実測 / 規律化はしない = 単なる手順の知見）
- **T4: 先頭固定を「retro 内の順序」から「次セッションのプランへの記載」まで延長する。** P4 の実測がその方向を示している

---

## Step 4: 持ち越し議題 3 件への回答

### (iii) 先頭固定の効果測定 — **測定できた。ただし主因は先頭固定ではない**

`retro-2026-08-17` の Try **5 件は全件実行された**。

| | 内容 | 実行日 | 経過 |
|:-:|:---|:---|:---|
| T1 | `-o addopts=""` の除去 | 2026-08-17（当日） | 0 日 |
| T2 | ガード 2 の検知器を 3 形へ拡張 | 2026-08-27 | 10 日 |
| T3 | 計器の隔離確認（R1 入場 = 取引 #16） | 2026-08-21 | 4 日 |
| T4 | 先頭固定の継続 | **本 retro** | — |
| T5 | P5 の検査 | 2026-08-29 | **12 日 / 5 回持ち越し** |

**先頭固定された項目（ガード 2 系 = T2）は実行された。されなかった T5 は 5 回持ち越された**——方向としては仮説を支持する。ただし **T5 が今日実行された契機は先頭固定ではなく、セッション冒頭のプランに独立タスクとして載せたこと**である。**「順序」が効くのは retro の中だけでなく、次セッションの作業計画に載るかどうかである**（→ T4）。

**旧基準 (a) の新規標本は 0 だった。** 本セッションは BUILDING であり、`phase-rules.md` の読みでは実況の発火点に一度も当たらなかった（P2 の食い違いはここに直結する）。**したがって 2026-08-27 以前の残置 6 本という標本は無傷で保全された** —— セッション冒頭に置いた「標本の固定」マーカーは意図どおり機能した（守るべきものが実際には減らなかったので、効果の実証にはなっていない）。

### (i) 「判定者が当事者」 — **本セッションで 4 度目。独立化が唯一機能した**

| 検出者 | Critical 検出数 |
|:---|:---:|
| L1 の自己チェック（`/release` Phase 2.5 の人手項目を自分で書いた直後） | **0** |
| 独立コンテキストの監査 | **2** |

2026-08-27 にガード 2 で採った手（**判定者を L1 から L3 へ移す**）と同型の効果が、別の領域でも観測された。**n=1 だが方向は一貫している。**

**ただし機械化の限界も同時に判明した**: 検出された Critical のうち、機械化できたのは 1 件（空ディレクトリの紹介）のみ。もう 1 件（**同じ規律が別ファイルに違う強度で書かれている**）は意味判断であり、機械化すると誤検出で計器が殺される。**独立の目は、機構では置き換えられない領域を持つ。**

### (ii) 安い通貨の抜け道 — **本セッションでは使われなかった。ただし塞がってもいない**

本セッションの通貨発行は **0**。

- **#4-fix**（`permission-levels.md` の重複削除）は、削る行に RFC 2119 キーワードを含まず、規律は §迷った場合 に継続するため **コピー削減であって退出ではない**と**着手前に**判定した（第二の限界 / 更正 #17 の教義）
- **R3 機構 #10** は予算外（R3）であり、§A に影響しない

**「通貨になるか」を作業前に明示的に判定する運用が働いた**のは前進である。一方、抜け道（skill に書いて R1 から剥がす経路）そのものは**依然として開いている**。本セッションはその経路を通る作業をしなかっただけであり、**問題は解決していない**。

> **なお本セッションで新機能 `planning` skill の復元を見送った理由の 1 つがこれである** —— 規律の入口を skill 空間に新設することは、審査中の経路に一件追加する行為になる。**判断は「却下」ではなく「順序」**（次セッションの #2 で所在の方針が決まってから）。

---

## Step 5: アクション

| # | アクション | 反映先 | 優先度 | 状態 |
|:-:|:---|:---|:---|:---|
| A1 | 実況発火点の食い違い（P2 / T1）を解消する | `CLAUDE.md`（**PM 級**） | **高** | **実施済**（2026-08-29 / ユーザー判断 = 案 (a) `PLANNING`・`AUDITING` 修飾の復元 / §A 不変 / **2026-08-27 に収載した `CLAUDE.md` 事前宣言義務の最初の行使**） |
| A2 | `docs/private/` と「規律 8 件」の所在整理 | 次セッション **最優先**（ユーザー指示 2026-08-29） | **高** | 次セッション |
| A3 | 「作業の締めに独立レビュー」の継続（T2） | 運用（既存常設指示） | 中 | 実施中 |
| A4 | 先頭固定を次セッションのプラン記載まで延長（T4） | 運用 | 中 | 本 retro の出力が次プランに載るかで検証 |
| A5 | 埋め込み Python は Write ツール経由（T3） | 手順の知見 | 低 | 規律化しない |

**条項は 1 つも追加しない**（A1 は既存条項の食い違い解消であり、追加ではない）。

---

## 参照

- `docs/artifacts/retro-2026-08-17.md`（前回 / T1-T5 の出所）
- `docs/private/fable-l3-protocol.md` §5.1・§5.4（発火点 / 標本の固定）
- `docs/artifacts/clause-gate-ledger.md`（R3 機構 #10 / §コピー削減の保守記録）
- `docs/artifacts/2026-08-27-distribution-docs-sweep.md`（S-3 の起票元）
- `.claude/rules/auto-generated/trust-model.md` §カウント単位（Step 2.5 の判定根拠）
