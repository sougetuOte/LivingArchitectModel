# 配布物ドキュメントの掃き（**検出のみ** / 2026-08-27）

**方針**: 検出だけを全域で行い、**修正は等級別に別途**（2026-08-22 の列挙ドリフト掃きと同じ形）。
**対象**: `README.md` / `CHEATSHEET.md` / `QUICKSTART.md` / `docs/internal/*.md`（8 枚 / 1,167 行）/ `docs/slides/*.html`（12 枚）
**方法**: 機械検出（参照実在 / 空ディレクトリ / 概念被覆 / 版表記 / 数値主張）→ **目視で歴史記述とノイズを分離**

> **本掃きの前提となった判断**: 機械検出は「古い語が残っている」ことしか言えない。**廃止された語が歴史記述として正当に残っている場合、消すのは誤り** —— これは本日 `Reflection` の処理で実証された（14 箇所のうち **5 箇所は残すのが正しかった**）。したがって本レポートは機械の出力をそのまま起票せず、分離した後の結果のみを載せる。

---

## 0. 本日先行して処理したもの（掃きの一部を前倒し）

| 対象 | 処理 |
|:-----|:-----|
| `Reflection`（**14 箇所 / 6 ファイル**） | 現行の仕組みとして説明していた **9 箇所 / 5 ファイル**を `gabriel probe` に置換（README ×2 / `docs/internal/02` ×1 / slides 日英 ×6）。**残り 5 箇所は歴史記述として保存**（`CHEATSHEET:204` / `docs/internal/06` の §6・§6.1・実測値・学術参照） |
| AoT 適用条件 3 軸の複製 | 表記揺れ（「2つ以上」/「2 つ以上」）を統一し、`decision-making.md` §適用条件 を**値の正本**と明示。`phase-rules.md` と `magi/SKILL.md` の 2 箇所が同じ値を持つことを条文に書いた |

`Reflection` は ADR-0007（Accepted **2026-07-02**）で置換済みであり、**約 2 か月間、配布物には旧名が残っていた**。

---

## 1. 事実の誤り（**SE 級** / 判断を要さず直せる）

| # | 箇所 | 内容 |
|:-:|:-----|:-----|
| **D-1** | `README.md:56` / `QUICKSTART.md:80` | **`commands/` を収録内容として記載しているが、ディレクトリは空**（skills へ移行済み）。README は「スラッシュコマンド（フェーズ制御 + 補助）」、QUICKSTART は「フェーズ制御・ワークフロー」と説明しており、**読者が存在しない機能を探しに行く** |
| ~~**D-2**~~ | ~~`docs/internal/00` / `02` の `src/` `tests/` `docs/memos/` 参照~~ | **取り下げ（誤検出 / 同日中に自己訂正）**。`00_PROJECT_STRUCTURE.md:10,13,40` は **既に「※配布先テンプレート枠 — LAM 本体には存在しない」と注記済み**であり、`docs/memos/` は**出力先の指定**（使用時に作られるため事前の実在を要しない）。`02:79` の `src/` も配布先前提の記述 |

> **D-2 取り下げの記録**: 本レポートは冒頭で「機械の出力をそのまま起票せず、分離した後の結果のみを載せる」と宣言しながら、**D-2 でその分離を怠った**（`Reflection` では 14 → 9 に絞り込んだのに、パス参照では「実在しない」をそのまま事実の誤りとして起票した）。**同じレポートの中で、宣言した手続きを守った項目と守らなかった項目が並んでいた。** 検出結果の起票前に「その参照は事前に実在すべきものか」を 1 問挟むこと。

---

## 2. 欠落（**PM 級** / 何を載せるかの判断が要る）

| # | 箇所 | 実測 |
|:-:|:-----|:-----|
| **M-1** | `README.md` | 現行 LAM を特徴づける語のうち、**あるのは `gabriel` の 1 語のみ**。**誕生ゲート / 権限等級 / 第 0 原則 / 委譲 / ハーネス / AutoMode / PM 級 = すべて 0 件** |
| **M-2** | `QUICKSTART.md` | 上記 9 語が**すべて不在**（0/9） |
| **M-3** | `README.md` | **版の記述が存在しない**。v5.1.0 を公開済みだが、README を読んでも現在版が分からない |

**M-1 の含意**: README 冒頭は「プロトコルセット」「能動的な守護者」という v4.x 時代の抽象語で自己紹介しており、**この 1 年で作った統治機構（承認等級 / 誕生ゲート / 3.5 層委譲 / 独立 adversarial probe）が front page に 1 つも現れない**。CHEATSHEET は比較的追随している（9 語中 6 語あり）ため、**欠落は「作っていない」ではなく「書いていない」**。

**判断の材料**（2026-08-26 調査 / `SESSION_STATE` 決定欄）: LAM は **governance 型の AI エージェント・ハーネス**に該当し、arXiv:2605.13357 の harness 定義 9 要素に **9/9 対応**。ただし runtime substrate は Claude Code 本体が担うため、狭義では「ハーネスの**統治層**」。**ルールに天井と通貨を持たせる設計（誕生ゲート）は調査範囲で他に存在しない。**

---

## 3. 歴史記述として正当（**修正しない**）

| 対象 | 判定 |
|:-----|:-----|
| `v4.0.0` 表記 8 箇所のうち「v4.0.0 新規」「v4.0.0 以降」「v3.x → v4.0.0 比較表」 | **導入時期の記録**であり、現行版の主張ではない |
| `Reflection` 残存 5 箇所 | **置換の経緯**（ADR-0007 / 実測値 / 学術背景）。消すと「なぜ gabriel なのか」が失われる |

> **例外 1 件（Info）**: `CHEATSHEET.md:63`「権限等級（PG/SE/PM）**v4.0.0 新規**」は、v5.1.0 の現在では「新規」の語が古びている。事実として誤りではないため Info に留める。

---

## 4. ノイズとして除外したもの

`Read/Glob/Grep`（パスではない）/ 外部リポジトリ名（`oraios/serena` 等）/ テンプレート内のプレースホルダ（`/absolute/path/to/your/project`）/ 環境依存パス（`/etc/claude-code/managed-settings.json`）。

---

## 5. 起票（等級別）

| 等級 | 件数 | 内容 | 着手条件 |
|:-----|:----:|:-----|:---------|
| **SE** | **1**（**2026-08-27 に処理済**） | D-1（`commands/` の記載を README・QUICKSTART から削除）。~~D-2~~ は誤検出のため取り下げ | 完了 |
| **PM** | **3** | M-1・M-2・M-3 | **ジャンルの名乗り方の決定**（front page に何を掲げるか） |
| Info | 1 | `CHEATSHEET:63` の「v4.0.0 新規」 | 任意 |

**この掃きは列挙ドリフトの 3 度目である**（2026-08-22 の条文 → 08-26 の CHEATSHEET → 本件）。ただし本件は性質が違う —— 前 2 回は「同じ列挙が複数箇所にあってズレた」だが、本件は「**製品が変わったのに配布物が追随しなかった**」であり、正本の一元化では防げない。**防ぐなら「リリース時に配布物を見る」工程が要る**（`/release` skill への追加が候補 / 本レポートは起票のみ）。

---

## 参照

- `docs/artifacts/2026-08-22-enumeration-drift-sweep.md`（前回の掃き / 形式の原型）
- `docs/adr/0007-*.md`（Reflection → gabriel の置換決定 / Accepted 2026-07-02）
- `docs/artifacts/clause-gate-ledger.md`（AoT 3 軸の複製に関する起票 / §コピー削減の保守記録）
