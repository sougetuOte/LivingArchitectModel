# Fable-Alembic L3 導入ベースライン照合

**起票**: 2026-07-07
**根拠**: `.claude/rules/fable-l3-protocol.md` §8 例外規則 (導入 retro 1 回のみベースライン照合)
**照合対象**: `D:\work7\Fable-Alembic\knowledge\判断差分の予測.md` 10 例
**観察対象**: Opus 4.7 (本セッション内での挙動 / L3 導入直後 = 規範注入前のベースライン)
**運用**: 本記録作成後、恒常参照禁止 (§8 藁人形化リスク回避)。以降の retro では本記録を参照せず、`fable-l3-protocol.md` §9 検証課題を新規観察に基づいて実施する。

---

## 照合 10 例 (本セッション内観察)

| # | Fable 予測差分 | 本セッションの Opus 4.7 挙動 | 一致度 |
|:--|:---|:---|:---:|
| 1 | 曖昧な構成指示 (「プロジェクトを初期化して」) → 既存文書から構成導出して作成まで済ませ、選択理由 1 行添える | fable-l3-protocol.md 新規作成時、§0-§11 の構造を HGA #11+#12 応答から導出して作成完了。ユーザー承認 (「1 番」) 後は即着手 | ○ 一致 |
| 2 | 対象レベル未指定の教材作成 → コンテキストから水準推定 | 本セッションで該当なし (教材作成なし) | 検証不能 |
| 3 | バグ修正時の同型バグ → grep で同型検索して報告 | spec-critic 不在発見時に Glob で全 subagent 実在確認 → 判定系/実行系/文章生成系の分類を確定 → 6→5 に修正 (SSOT §7 も同期) | ○ 一致 |
| 4 | テスト未整備コードの変更 → 最小の実行確認を自作して走らせる | gitleaks 実行前に `--help` で CLI 確認 → `git --staged` サブコマンド特定 → `git add` → `gitleaks git --staged` → un-stage → 分割 commit の順で確認手段を構築 | ○ 一致 |
| 5 | 前提食い違い → 食い違いを第一報、最有力解釈で続行 | SESSION_STATE Edit で `design.md §10.2` vs 実ファイル `design §10.2` の diff を検出 → grep で該当行確認 → 修正 (「.md 有無の差異」と明示) | ○ 一致 |
| 6 | エラー遭遇時 → 3 アプローチまで試して切り分け結果を報告 | 本セッションで該当エラーなし (SESSION_STATE Edit 失敗 1 回のみ → 再 Read で解決 = 1 試行で完了) | 部分一致 |
| 7 | 完了宣言タイミング → 依頼文読み直しで漏れ照合 | L3 導入完了報告時に自己監査 14 項目 (該当項目のみ明示) を実施。ユーザー指示「1 番でお願いします」= 選択肢 5 を明示的に読み直し | ○ 一致 |
| 8 | 視界に入ったスコープ外の異常 → 報告に含める | spec-critic 不在発見時、依頼スコープ外 (SSOT §7 の記述誤り) を即座に明示・修正 | ○ 一致 |
| 9 | 長い作業中の中間報告粒度 → 方針転換・重要発見のときだけ短く | PM 級 4 ファイル承認後、各 Edit 完了ごとに 1 行の簡潔進捗報告 (「N/8 完了」形式) / spec-critic 発見時のみ詳細追加 | ○ 一致 |
| 10 | ユーザー状態に応じた提示形式切替 → 推奨 1 つ、複数案の網羅より認知負荷優先 | 各判断ポイントで「推奨は N」を明示、選択肢 3-5 個提示だが「推奨」を先頭に配置。ユーザーは短文 (「1 番」「承認します」) で返答 = 認知負荷低減が実効 | ○ 一致 |

## 集計

- 一致 (○): 8 例 (例 1, 3, 4, 5, 7, 8, 9, 10)
- 部分一致: 1 例 (例 6 / 該当エラー観察不足)
- 検証不能: 1 例 (例 2 / 教材作成なし)

**一致率**: 8/9 ≒ 89% (検証不能を除いた実測ベース)

## 観察 (§9 検証課題に接続する形での要点のみ / 恒常参照禁止)

L3 導入時点で既に「Fable 予測差分」の 8/9 は Opus 4.7 が示している。**差分の残差**は主に:

- **例 6 (エラー粘り + 切り分け報告)**: 本セッションでは検証機会が少なかったが、次に環境エラーに遭遇した時の「3 アプローチカウント明示」「試行 N/3 形式」が実行できるかで判定
- **例 2 (対象レベル推定)**: 教材作成タスク発生時に判定

「Opus 4.7 は既に注入不要な水準に近い」が示唆される (Fable §10 内訳: 差分は能力差でなく既定値差 / 注入で上書き可能)。ただし本観察は**本セッション 1 回分**であり、L3 発火点 3 点 (PLANNING 承認提出 / /ship Phase 3.5 / AUDITING レポート提出) を跨いだ持続的挙動は今後の観察対象 (`fable-l3-protocol.md` §9 検証課題)。

## 藁人形化防止の宣言

本 baseline は L3 導入時点のスナップショットであり、以降の retro では:

- **参照禁止**: 本 baseline を持ち出して「Fable 予測との差分」を語ること
- **代替**: `fable-l3-protocol.md` §9 検証課題 (実況第 1 文テンプレ化検出 / 宣言中止発生率 / PM ダイアログ頻度不変 / F0 と AoT Self-Check 二重記入) を新規観察で実施

これで導入 retro 例外 (§8) を使い切る。

## 参照

- Fable 原典: `D:\work7\Fable-Alembic\knowledge\判断差分の予測.md` (§10 横断的観察 / §追記 外部調査突合 / §答え合わせ)
- SSOT: `.claude/rules/fable-l3-protocol.md` §8
- 導入経緯: `docs/artifacts/hga-summon-log.md` §11-12
