# gabriel adversarial probe 記録（2026-08-26 / セッション 25 / 2 件）

**契機**: 2026-08-26 の quality-auditor 監査が「**probe の実行記録がリポジトリに残っていない**（台帳への転記のみ）」を未確認事項として指摘した。本日の会計判定は 2 件の probe verdict に依存しており、**根拠の所在が台帳の自己申告しかない**状態だった。本ファイルはその手当てである。

> **位置づけ**: いずれも MAGI 合議（AoT + MELCHIOR/BALTHASAR/CASPAR）ではなく、**単発の判断に対する adversarial verification 単体**の起動である。`decision-making.md` §gabriel Adversarial Probe の rubric（論理的一貫性 / 仕様整合 / リスク見落とし / 前提検証 / 境界条件）は適用したが、Divergence / Debate ログは存在しない。

---

## Probe #1 — 「本セッションの R1 編集 4 件は予算中立か」

**対象**: L1 が単独で下した会計判定（B 群の条文修正 4 件を「承認済み実装の記述化ゆえ予算中立」とした判断）。
**構造的リスク**: L1 が自分の編集を自分で会計分類している（会計者と被監査者が同一）。

| フィールド | 値 |
|:---|:---|
| verdict | **refuted** |
| severity | **warning** |
| affected_atoms | `["A"]` |
| recommended_action | re-magi |
| confidence | **0.6** |
| 処理 | `resolve_action()` により **`annotate_warning`**（refuted × warning）|

**論点別の結果**:

| 論点 | 結果 |
|:---|:---|
| A: 「実装が既にそうなっている」は R1 初出を予算外にする理由になるか | **通らなかった** |
| B: `yum` / `service` / `shutdown` の Deny 行追加は新規の禁止条項か | 棄却（deny 16 件に既存 = 記述漏れの解消）|
| C: `mv` の Ask→Deny / `.claude/states/` の緩和方向 | 棄却（掃きが 8/22 時点で実装先行を記録済）|
| D: §A 不動を「反証用の計器」としたのは事後正当化か | 棄却（取引 #15 → 更正 #17 の教訓を正しい向きで適用）|

**A の中身（逐語）**: 台帳 §2 の非対称テストは「その指令は **R1 のどこかで**まだ拘束しているか」という R1 内限定の文言であり、**退出方向にしか明文化されていない**。今回の 4 件は「R1 prose には一度も現れていなかった規律を、`settings.json` / hook という **R1 外**の機構が既に執行していた」ことを根拠に予算中立としており、既存取引（#9 / #18-#20 = いずれも R1 内移動）のどれとも異なる。

**帰結**: ユーザー判断により **§射程内記述（ゼロコストの入場）** を新設（`clause-gate-ledger.md`）。判定は「**その条文を消したら実際の振る舞いが変わるか**」の 1 問。

---

## Probe #2 — 「新設カテゴリが同日 2 度目の適用で緩められていないか」

**対象**: L1 が §D 在庫 #5 を射程内記述カテゴリで「無料」と判定し閉じた件（コミット `3d76f63`）。
**構造的リスク**: **制度の作者が、新設の数十分後に自分の案件へ適用している。**

| フィールド | 値 |
|:---|:---|
| verdict | **refuted** |
| severity | **critical** |
| affected_atoms | `["A", "B", "C"]` |
| recommended_action | re-magi |
| confidence | **0.72** |
| 処理 | `resolve_action()` により **`re_magi`**（refuted × critical × retry_count=0）→ **実際はユーザー裁定により差し戻し (b) を実施** |

**決定的だった指摘（論点 B / 逐語要約）**: §D 在庫 #5 自身（2026-08-22 記載）は、**L1 が「決め手」として持ち出した `_hook_utils.py:61-72` のコメントを既に参照した上で**、「承認ゲートは hook が執行し実挙動は安全側だが、**条文上 L1 は `CLAUDE.md` 編集の事前宣言義務を負っていない**」「拡張は所在の変更ではなく**要否の判定**」と明記し、**有償入場に分類していた**。同一証拠に基づき 4 日後に無償と再分類しており、**新事実はない**。

**論点 C**: 同日実施の参照化 3 箇所と結合することで、事前宣言義務の実質適用範囲が**会計上ゼロで拡張**されていた（正本表を無料で直せば、参照する側の義務範囲が自動で広がる）。

**帰結**: **ユーザー裁定 = (b)**。参照化（コピー削減 = 無料）は残し、`CLAUDE.md` の追加 2 箇所のみ差し戻し。§D 在庫 #5 は**未消化のまま在庫に戻した**。併せて射程内記述カテゴリに成立要件 1 項（**先行判定の確認**）を追加した。

---

## 2 件を通しての観測

**同日中に、同型の逸脱が 2 回起きた。** 1 回目（warning）の対処として制度を新設し、その制度を使って 2 回目（critical）をやった。

**カテゴリの不在が問題だったのではなく、分類者が当事者であることが問題だった。** 台帳が別の文脈で警戒していた構造（「L1 が退出候補を提示する作業は、自分を縛る条項を自分で選ぶ構造を持つ」）と同一である。**制度を足すと逸脱の language が増える**という逆効果が実測された。

**有効だったのは制度ではなく独立検証である。** 2 件とも L1 の自己点検では出ず、別コンテキストの gabriel が拾った。なお本日の canary（`disallowedTools` の確証）により、**gabriel は実際に読み取り専用で動作している**ことが確認済みであり、この検証系の独立性は frontmatter の宣言ではなく実測に裏づけられている。

## 参照

- `docs/artifacts/clause-gate-ledger.md` §射程内記述（ゼロコストの入場）/ §D 在庫 #5
- `docs/artifacts/2026-08-22-enumeration-drift-sweep.md` §1 / §6
- `.claude/rules/decision-making.md` §gabriel Adversarial Probe
