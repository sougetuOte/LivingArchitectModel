# M-1 配布カタログ

| 項目 | 内容 |
|:-----|:-----|
| Milestone | M-1（Opus 5 移行 + 規律の条項トリアージ） |
| 対応 Task | W4-M1-T3 |
| 対応仕様 | `docs/specs/m-1-opus5-migration/design.md` §8.3 / requirements.md FR-18 |
| 作成日 | 2026-07-26 |
| ステータス | 作成完了（配布実行は W4-M1-T4 でユーザー承認後に別途実施） |

---

## 位置づけ（配布 2 経路のうち「規律」側）

M-1 の成果物は 2 経路で配布する（design.md §8.3 / ADR-0011 決定 5）。

| 配布対象 | 経路 |
|:---------|:-----|
| skills / agents の変更（W3 成果物） | ADR-0010 の plugin チャネル（`lam-harness` の version bump） |
| **規律の変更**（`CLAUDE.md` / `.claude/rules/` / `model-roster.md` パターン） | **本カタログ**（正本 `docs/artifacts/m-1-distribution-catalog.md` / 配布用コピーは LAM リポジトリ外） |

**本カタログは後者のみを扱う**。`.claude/skills/magi/SKILL.md` を含む skill/agent 側の変更はここに 1 行も含めない（下記「対象外（除外した条項）」参照）。

入力データは `docs/artifacts/m-1-clause-ledger.md`（`^| \`` 形式のデータ行 58 件 / `grep -c` 実測値）。カタログの各行は「他プロジェクトが取捨選択する単位」に台帳の条項 ID を集約したものであり、集約元との突合は本ファイル末尾「集約元条項ID対応表」で行う。

---

## 本体表（7 列 / 閉集合）

| 変更項目 | 種別 | LAM 固有度 | 必要 harness バージョン | 前提モデル世代 | 依存 | 判断軸 |
|:---------|:-----|:-----------|:--------------------------|:-----------------|:-----|:-------|
| ① `model-roster.md` 新設（モデル名束縛の単一 SSOT 化） | SSOT 退避 | **中**（要調整）— 「モデル名を 1 ファイルに集約する」設計思想は移植可能だが、収録内容（LAM 固有の層定義 L1/L1.5/L2/L3/HGA、Fable/Opus/Sonnet/Haiku の個別単価表）は LAM 固有であり差し替えが必要 | 不問（`.claude/rules/` 配下の平文ファイルで plugin 配布対象外） | 非依存（roster の目的自体が世代交代の吸収であり、現行値が Opus 5/Sonnet 5/Haiku 4.5/Fable 5 なのは単なる現在値） | なし | モデル世代交代のたびに複数ファイルを横断編集しているか？ |
| ② `CLAUDE.md` 内のモデル名直書きを層記号 / upstream 実装詳細への言及に圧縮 | 圧縮 | **中**（要調整）— 「最上位の常時ロード文書にモデル名をハードコードしない」という規律は移植可能だが、置換先の記号（L1/L2/L3/HGA）は LAM 固有の層定義に依存 | 不問 | 非依存 | ①（roster が先に存在しないと層記号への置換先が定まらない） | 常時ロードする最上位文書（CLAUDE.md 相当）にモデル名がハードコードされていないか？ |
| ③ Fable-Alembic 自己監査 14 項目のうち発火実績ゼロの条文を削減・実測発火ありの条文を圧縮 | 削減＋圧縮（混在） | **高**（移植不可）— 自己監査 14 項目は Fable-Alembic（外部プロジェクト）由来のチェックリストであり、同ヒューリスティクスを L3 運用しないプロジェクトには対応する原典自体が存在しない | 不問 | 非依存 | なし | Fable-Alembic の自己監査 14 項目のような外部ヒューリスティクスを L3 運用しているか？ |
| ④ 60 秒実況関連ガードの重複条文を削減（subagent 完了報告時の実況省略 + テンプレ化ガード 1/3/6） | 削減 | **高**（移植不可）— 「60 秒実況」自体が Fable-Alembic の受け手体験シミュレーション概念であり、この制度を持たないプロジェクトには適用対象がない | 不問 | 非依存 | なし | 60 秒実況のような受け手体験シミュレーションを制度として運用しているか？ |
| ⑤ F4（全体検証 5 点）の重複記載を一本化（`fable-l3-protocol.md` と `phase-rules.md` の二重記述 → 証拠要求 1 行へ圧縮） | 削減 | **高**（移植不可）— F4 は F0-F4 という Fable-Alembic 実行プロトコル由来の枠組みであり、同枠組みを持たないプロジェクトには「重複を 1 行に圧縮する」対象自体が存在しない | 不問 | 非依存 | なし | 同一のチェックリスト（F4 相当）を 2 つ以上のルールファイルに重複記載していないか？ |
| ⑥ `phase-rules.md` BUILDING 節の F0-F4 関連条文を `fable-l3-protocol.md` への参照に一本化（F0 実施済み条文 / F1・F2 の関係 / 試行上限の重複記述を削減） | 削減 | **高**（移植不可）— F0-F4 という段階的実行プロトコルの枠組み自体が Fable-Alembic 由来であり、同枠組みを持たないプロジェクトでは参照一本化の対象（フレームワークの節）が存在しない | 不問 | 非依存 | なし | F0-F4 のような段階的実行プロトコルの枠組みを採用しているか？ |
| ⑦ BUILDING の TDD 品質チェック（R-1/R-4）+ 仕様同期ルール（S-1/S-3/S-4）を詳細列挙から要点圧縮 | 圧縮 | **中**（要調整）— 「仕様とテストの文字単位一致を確認する」「実装変更時に仕様書を即時更新する」という規律自体は汎用的だが、R-1/R-4/S-1/S-3/S-4 という付番は LAM 内部のルールコードであり読み替えが必要 | 不問 | 非依存 | なし | TDD/仕様同期ルールに固有のルールコードを付番し、詳細列挙が冗長化していないか？ |
| ⑧ AUDITING の網羅的コード品質・明確性チェックリスト（命名/SRP/エラーケース/ネスト三項/ワンライナー/デバッグ容易性）を削減 | 削減 | **低**（そのまま可）— 命名の意図表現・SRP・エラーケース網羅・ネスト回避等は言語非依存の汎用コード品質項目であり、専用ガイドライン（`code-quality-guideline.md` 相当）へ一本化する構造もそのまま移植可能 | 不問 | 非依存 | なし | コード品質チェックリストがフェーズ規律と専用ガイドラインの 2 箇所に重複していないか？ |
| ⑨ AUDITING のドキュメント・アーキテクチャチェック（仕様差異/ADR反映/循環依存/TODO棚卸し）を 4 点へ圧縮 | 圧縮 | **低**（そのまま可）— 仕様との差異／ADR 反映／循環依存／TODO 棚卸しの 4 点はプロジェクト形態を問わず汎用的なドキュメント整合性チェックであり、そのまま移植可能 | 不問 | 非依存 | なし | AUDITING のドキュメント整合性チェックが冗長な列挙になっていないか？ |
| ⑩ `CLAUDE.md` Active Retrieval の 2 条文を運用移管（機構化せず retro 監視 + 失効日 2026-10-25 へ） | 運用移管 | **中**（要調整）— 「以前の記憶だけで回答しない」という規律自体は汎用だが、「運用移管」という決定様式（`trust-model.md` の検出イベント/閾値 2 への再機構化経路）は LAM の TDD 内省パイプライン v2 という既存機構に依存しており、同機構を持たないプロジェクトでは同型の移管判断を再現するのに別途設計が必要 | 不問 | 非依存 | なし | 発火が無計装でも気づける規律を、機構化コストに見合わず維持していないか？ |
| ⑪ `CLAUDE.md` 自律実行の既定の作業手順書明記（重複記述）を削減 | 削減 | **高**（移植不可）— 参照先が `fable-l3-protocol.md` §6.1（F0-F4 省略基準）という Fable-Alembic 由来の文書であり、同文書を持たないプロジェクトでは削減後の状態（参照のみ）が成立しない | 不問 | 非依存 | なし | 憲章ファイルと詳細ルールファイルに同一の作業手順が二重記載されていないか？ |
| ⑫ `terminology.md` §4「文書参照・表記の精度」3 項目の新設（圧縮形で実施） | 圧縮（未執筆条項の新規実施） | **低**（そのまま可）— 「相互参照は見出し表記を使い行番号を使わない」「表・節番号の挿入は末尾追加を既定」「成果物ファイル名の日付は起草日/実行日で使い分け」の 3 規則は文書執筆一般に適用可能な汎用プラクティス | 不問 | 非依存 | なし | 文書間の相互参照に hardcode な行番号を使っていないか？ |
| ⑬ `planning-quality-guideline.md` §1 危険な単語リストへ「暗黙前提」項目を追加（圧縮形で実施） | 圧縮（未執筆条項の新規実施） | **低**（そのまま可）— 「実装者が暗黙裡に補う前提を明示せよ」という要件レビューの一般原則であり、言語・ドメインを問わず適用可能 | 不問 | 非依存 | なし | 要件レビュー基準に暗黙前提の明示化という観点が抜けていないか？ |
| ⑭ `model-delegation-prompting.md` §2 必須 7 項 第 2 項へ下方向フェンスを追記（scratchpad 等への書込禁止の明記） | 保全（既存条文への追記） | **中**（要調整）— 「委譲先モデルの成果物書込先を明示的にフェンスする」という委譲プロンプト設計原則は汎用的だが、具体的な禁止対象（scratchpad ディレクトリパス等）は各プロジェクトの実行環境に依存し読み替えが必要 | 不問 | **Sonnet 5**（挙動デルタ 3「実装詳細の over-delivery」対策として追記 / `model-roster.md` §3） | なし | Sonnet 系モデルへの委譲プロンプトで成果物の書込先を明示的にフェンスしているか？ |
| ⑮ `hga-summoning.md` §primary_sources の書式例へ追記 | 保全（既存条文への追記） | **高**（移植不可）— HGA 型 Fable 召喚という体制自体が LAM 固有（ADR-0009）であり、召喚を行わないプロジェクトにはこの追記の適用対象が存在しない | 不問 | 非依存（Sonnet 系 subagent の下調べ委譲で実測された傾向だが特定世代限定ではない） | なし | 下調べ委譲で subagent が誤ったローカル文書を一次資料と誤認するリスクを抱えているか？ |
| ⑯ ロード条件（`paths:`）条件付与の見送り決定の記録（`CLAUDE.md` Python 呼び出し規約 4 件 + `terminology.md` §4.5 / **実質的なファイル変更を伴わない判断記録のみ**） | 見送り（非変更） | **高**（移植不可）— 判断根拠が「W1 のロード条件判定は条項単位、`paths:` はファイル単位」という LAM 独自のトリアージ機構の粒度ギャップに依存しており、同種のトリアージ機構を持たないプロジェクトには適用文脈が存在しない | 不問 | 非依存 | ⑫（`terminology.md#4.5-p01` の `paths:` 見送り判断は⑫の内容実施が先に存在することが前提） | 条件付きロード機構がファイル単位、条項判定が条項単位という粒度ギャップを抱えていないか？ |

---

## 集約元条項ID対応表（台帳との突合）

台帳（`docs/artifacts/m-1-clause-ledger.md`）の生データ行は `grep -c "^| \`" docs/artifacts/m-1-clause-ledger.md` で **58 件**。以下は本カタログの各行が集約した台帳側の条項 ID と、台帳内の掲載箇所（複数箇所に同一 ID が再掲されている場合はその都度 1 件としてカウントする）。

| 行 | 集約元の条項 ID（台帳での掲載行 / 件数） |
|:--:|:--------------------------------------------------------------|
| ① | `model-delegation-prompting.md#1-m01`（L37）/ `hga-summoning.md#envelope-m01`（L38）/ `hga-summoning.md#envelope-m02`（L39）/ `hga-summoning.md#コスト構造-m01`（L40）/ `CLAUDE.md#Execution-Modes-01`（L41）/ `CLAUDE.md#作業体制-model-id-m01`（L42）/ `CLAUDE.md#モデル運用-m03`（L44） — **7 件** |
| ② | `CLAUDE.md#担当層の判断基準-model-id-m02`（L43）/ `CLAUDE.md#AutoMode-classifier-m04`（L45） — **2 件** |
| ③ | `fable-l3-protocol.md#4-01`〜`#4-06`（L46-51） — **6 件** |
| ④ | `fable-l3-protocol.md#5.1-01`（L52）/ `#5.1-02`（L53）/ `#5.4-01`（L54）/ `#5.4-03`（L55）/ `#5.4-06`（L56） — **5 件** |
| ⑤ | `fable-l3-protocol.md#6.6-01`（L57）/ `#6.6-02`（L58）/ `phase-rules.md#AUDITING-F4-02`（L72）/ `-03`（L73）/ `-04`（L74）/ `-06`（L75） — **6 件** |
| ⑥ | `phase-rules.md#BUILDING-必須-02`（L59）/ `#BUILDING-F0-01`（L60）/ `#BUILDING-F1F2-01`（L61）/ `#BUILDING-試行上限-01`（L62） — **4 件** |
| ⑦ | `phase-rules.md#BUILDING-TDD品質チェック-01`（L63）/ `-02`（L64）/ `#BUILDING-仕様同期ルール-01`（L65）/ `-02`（L66）/ `-03`（L67） — **5 件** |
| ⑧ | `phase-rules.md#AUDITING-必須-01`（L68）/ `#AUDITING-コード品質チェック-01`（L69）/ `-02`（L70）/ `-03`（L71）/ `#AUDITING-コード明確性チェック-01`（L76）/ `-02`（L77）/ `-03`（L78） — **7 件** |
| ⑨ | `phase-rules.md#AUDITING-ドキュメントアーキテクチャ-01`（L79）/ `-02`（L80）/ `-03`（L81）/ `-04`（L82） — **4 件** |
| ⑩ | `CLAUDE.md#Active-Retrieval-01`（L83）/ `#Active-Retrieval-02`（L84） — **2 件** |
| ⑪ | `CLAUDE.md#自律実行の既定-03`（L85） — **1 件** |
| ⑫ | `terminology.md#4.5-p01`（主台帳 L86 / 付表 L102 再掲） — **2 件** |
| ⑬ | `planning-quality-guideline.md#1.5-p01`（主台帳 L87 / 付表 L103 再掲） — **2 件** |
| ⑭ | `model-delegation-prompting.md#boundary-p01`（付表 L104） — **1 件** |
| ⑮ | `hga-summoning.md#primary_sources-p01`（付表 L105） — **1 件** |
| ⑯ | `CLAUDE.md#Python-Invocation-Context別form-01`〜`-04`（見送り決定表 L118）/ `terminology.md#4.5-p01`（見送り決定表 L119 再掲） — **2 件** |
| **合計** | **57 件**（58 件中 1 件は下記「対象外」により除外） |

### 対象外（除外した条項）

| 条項 ID | 台帳掲載箇所 | 除外理由 |
|:--------|:-------------|:---------|
| `magi-SKILL.md#Phase0-Grounding-p01` | 付表 L106 | 反映先が `.claude/skills/magi/SKILL.md`（skill ファイル）であり、W4-M1-T3 完了条件「skills / agents の変更はカタログに含めない（plugin チャネル側）」により本カタログの対象外。配布は `lam-harness` plugin の version bump（W4-M1-T4）で行う |

**カバレッジ**: 台帳データ行 58 件中、**57 件**（98.3%）を①〜⑯のいずれかに集約。残り **1 件**（`magi-SKILL.md#Phase0-Grounding-p01`）は上記理由により本カタログの対象外として明示的に除外し、カバー不能条項として全件列挙した（除外はこの 1 件のみ）。

### 配布 2 経路が覆わない成果物（FR-18 の既知ギャップ / L1 検収で検出 2026-07-26）

FR-18 の配布 2 経路は「**規律の変更**（本カタログ）」と「**skills / agents の変更**（plugin チャネル）」で構成される。しかし M-1 の成果物には**どちらにも属さない機構**が存在する。

| 成果物 | 種別 | カタログ | plugin | 現状 |
|:-------|:-----|:--------:|:------:|:-----|
| `.claude/scripts/verify_model_reference.py` | スクリプト | 対象外（規律条項ではない） | 対象外（skill でも agent でもない） | **配布経路なし** |
| `.claude/tests/scripts/test_verify_model_reference.py` | テスト | 同上 | 同上 | **配布経路なし** |
| `.claude/tests/rules/test_update_model_skill.py` | テスト | 同上 | 同上 | **配布経路なし** |

これは実行漏れではなく **FR-18 の設計上の隙間**である。カタログ行 ①（`model-roster.md` 新設）を他プロジェクトが採用しても、**その SSOT を守る drift 検査機構は付いてこない**。規律だけが移り、規律を守らせる機構が残る形になる。

W4-M1-T5 retro の議題とする（本カタログでは判断せず、事実として記録するに留める）。

> `.claude/skills/update-model/SKILL.md`（W4-M1-T1 の成果物）は skill であるため plugin チャネル側に属する。ただし `lam-harness` plugin に当該 skill は現存しないため、配布するなら**新規追加**となる（W4-M1-T4 の判断対象）。

---

## LAM 固有度 内訳

| 判定 | 件数 | 該当行 |
|:-----|:----:|:-------|
| 高（移植不可） | **7** | ③④⑤⑥⑪⑮⑯ |
| 中（要調整） | **5** | ①②⑦⑩⑭ |
| 低（そのまま可） | **4** | ⑧⑨⑫⑬ |
| **合計** | **16** | — |

---

## 参照

- `docs/artifacts/m-1-clause-ledger.md`（本カタログの入力 / 58 データ行）
- `docs/specs/m-1-opus5-migration/design.md` §8.3（配布 2 経路・7 列閉集合の定義）
- `docs/specs/m-1-opus5-migration/tasks.md` W4-M1-T3（本ファイルの完了条件）/ W4-M1-T4（配布実行・ユーザー承認ゲート）
- `docs/adr/0011-clause-triage-and-model-generation-governance.md` §決定 5（LAM 固有度 3 値の定義: 高=移植不可 / 中=要調整 / 低=そのまま可）
- `docs/adr/0010-global-claude-assets-governance.md`（plugin チャネル側の境界 / I-1, I-6）
- `.claude/rules/model-roster.md`（①②⑭の移動先・LAM 固有度判定の裏付け）
- `.claude/rules/fable-l3-protocol.md`（③④⑤⑥⑪の出典 / Fable-Alembic L3 運用の根拠）
- `.claude/rules/hga-summoning.md`（⑮の出典 / HGA 型召喚体制の根拠）
