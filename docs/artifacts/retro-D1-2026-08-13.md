# Retro: Milestone D-1「配布境界の決定」（2026-08-13）

**参加**: L1（Fable 5 / 本セッションからトップ運用）+ ユーザー
**対象**: W1（判定）/ W2（反映）/ W3（締め）
**Done 判定の根拠**: pytest 795 passed + 14 skipped / §A = 61 ≤ 80 / CHANGELOG 記録済

> 状態: **確定**（2026-08-13 ユーザーレビュー済 / tasks.md §3 T9「L1 + ユーザー」）

---

## §1 必須議題への回答（tasks.md §3 T9 / 成功基準 6）

### 議題 1: WC-11 防御の成否 —— 「次も同じやり方で」と言える状態になっていないか

**判定: 防御は成立している。ただし誘惑は実在した（記録する）。**

- D5 で誕生ゲートに足したのは**新規条項の誕生時 1 回の判定のみ**。定期実行の契機（時計）は作っていない
- 一方で、W1 の「証拠表 → ユーザー判定 → 反映」というフローは**うまく機能した**（15 件を 1 セッションで通し、判定の中断も 2 回目の試行で解消した）。「skills / agents / hooks も同じ型で回せばよい」と言いたくなる状態には**なっている**。これこそ WC-11 の入口なので、明文で止める: **後続の判定対象（skills 約 33 / agents 12 / hooks 7）は、配布列 5 パターンも証拠表形式も流用できるとは限らない**。後続 Milestone を立てる場合は判定軸から都度設計する（本 retro を「手順書」として引用しないこと）

### 議題 2: WC-10 の点検 —— マニフェスト・検査群・分類学が生えていないか

**判定: 生えていない。**

- マニフェスト: 作っていない。境界の表現は配置 1 つのみ（`docs/private/` にあるか否か）
- 検査群: 新設ゼロ。ban テストの所有者ゲートは**既存の `_IS_AUTHORING_ENV` シグナルの転用**であり、新しい検査対象を持たない。§A の検査も既存の `test_clause_gate_ledger.py` がそのまま機能した（design §5 の予告どおり「出口の不動点が既に存在する」）
- 分類学: P/X/S の判定結果は evidence 1 ファイルに閉じている。frontmatter・メタデータ列・分類表への転記は行っていない
- 新規ファイルは 2 件のみ: `docs/private/d-1-split-extracts-2026-08-13.md`（記録であって検査ではない）と本 retro

### 議題 3: 手 4 との分離 —— T4 で「要るか」を問い始めた瞬間が無かったか

**判定: 無かった。ただし境界上の判断が 1 件あった（透明化する）。**

- T4 の 5 件はすべて「誰のものか」軸で実施し、各件 1 行の軸記録を extracts に残した（tasks.md T4 完了条件）
- 境界上の判断: `hga-summoning.md` 行 122（envelope 定義の `model-roster.md` §4 への参照）を「P 資産への参照だから据え置き」と判断した。これは「誰のものか」軸の内側の判断（参照先がユーザー判定 P である事実に基づく）であり「要るか」ではない —— が、判断の瞬間があったこと自体をここに記録する
- 抜いた内容はすべて逐語で extracts に保存されており、判定の可逆性は保たれている

### 議題 4: D-1 の残余（後続への引き継ぎ）

| # | 残余 | 引き継ぎ先 |
|:-:|:--|:--|
| 1 | **skills（約 33）/ agents（12）/ hooks（7）の配布判定は未実施**（FR-4 のスコープ外） | 後続 Milestone（議題 1 の注意つき） |
| 2 | **`_OUTBOUND_WRITE_BAN_ROOTS` の無条件実在検査は置けない**（hook がスコープ外のため / design §5「置かない」根拠記録済） | hooks を対象に含む後続 Milestone |
| 3 | **T6 検証 4（新規セッションで `docs/private/` が注入されないことの目視確認）が未実施** | **次セッション冒頭で L1 が確認する**（SESSION_STATE に記載） |
| 4 | ban テストの所有者ゲートは「所有者が `SESSION_STATE.md` を削除すると静かに skip」する既知の弱点を引き継ぐ（受忍済 / 変更なし） | — （記録のみ） |
| 5 | `docs/private/` の条文を参照する製品側ファイル（`phase-rules.md` 等）は、配布先で参照先が削除されうるポインタを持つ。**規律本体は製品側に残っているため機能的な害はない**が、配布物の見栄えとしては残余 | README 配布手順の改訂（design §7 で後続送り済み）と同じ波 |

---

## §2 実測メモ（次に効く数字）

- **§A: 80 → 61**（X 1 件 = 指令 19 の退出のみで達成。S 5 件の分割は指令数を 1 も動かさなかった —— 抜いたのがパス・帰属・数値でありキーワードを含まなかったため。「配布境界の是正はゲージにほぼ写らない」の実例）
- 判定所要: 15 件をテキスト一括提示で 1 往復（前セッションの AskUserQuestion 4 件ずつは 0 件で不発）。**提示形式が律速だった**のは tasks.md T2 注記の予想どおり
- 付随して直した既存 drift 1 件: GitHistoryParser の Wave 検出 0 件（rule-002 同族 4 件目 / Task ID からの導出で恒久解）。**「Wave N」散文がコミット文体から消えても計器が追随する**形にした（rule-001 観測 #5 の「計器が対象を変形させる」問題への部分回答でもある）

## §3 参照

- 判定: `docs/artifacts/d-1-evidence-2026-07-28.md` §6
- 台帳: `docs/artifacts/clause-gate-ledger.md` §A / §B 取引 #13
- 設計: `docs/specs/d-1-distribution-boundary/`（requirements v2.0 / design v0.2 / tasks v0.1）
- 抜き出し集約: `docs/private/d-1-split-extracts-2026-08-13.md`
