# Retro 2026-08-17（セッション 20-21 / 議題 5 件）

**スコープ**: セッション 20（2026-08-15）〜 セッション 21（2026-08-17）/ commits `68d1bed`..`e66d96f`（8 件）
**議題選択**: ユーザー判断 **(a) = 5 件すべて**。ただし **#1 を先頭固定・後続のために圧縮しない**という条件付き（理由: #1 は 3 回とも「複数議題のうちの 1 件」として持ち越されており、順序を変えなければ持ち越し条件を再現する）
**Milestone**: なし（D-1 は 2026-08-13 クローズ）

---

## Step 2: 定量

| 指標 | 値 |
|:-----|:---|
| 実装タスク | 1（R3 機構 #7 = 条件ロード曝露検出） |
| テスト追加 | **24**（`test_compaction_exposure.py`） |
| 全数 | **819 passed / 14 skipped / 0 failed** |
| 台帳 §A（常駐指令数） | **61**（不変 / 天井 80 / 余裕 19） |
| R1 入場 | **0** / 交換 **0** |
| R3 機構 | 6 → **7** |
| R4 在庫 | 1 → **2** |
| PM 級ファイル改訂 | 2（`model-roster.md` / `hga-summoning.md`） |
| 破棄した条項軸 | **1**（実 $ envelope 月 $10-40） |
| 承認イベント | **1**（段 2 で 3 判定を束ねた / HGA #15） |

---

## Step 2.5: TDD パターン分析 — **今日の遷移は記録されていない**

`.claude/tdd-patterns.log` の最終エントリは **2026-07-26**。本日 Red（20 failed）→ Green（24 passed）を回したにもかかわらず、**FAIL→PASS 遷移は 1 件も記録されなかった**。

### 原因（実測で特定）

`pyproject.toml` の `addopts = "--junitxml=.claude/test-results.xml -m '...'"` が、`trust-model.md` の定める唯一のデータ源（JUnit XML）を生成している。本セッションの TDD 実行はすべて **`-o addopts=""`** を付けており、これが XML 生成ごと無効化していた。対照実験で確認済（`-o` なしで走らせると `.claude/test-results.xml` が更新される）。

### 出所 — **条件ロード規範が処方したコマンドだった**

`-o addopts=""` は L1 の思いつきではない。**`.claude/rules/subprocess-encoding-convention.md` §検証コマンド**が逐語でそう書いており、本セッションで `.py` を Read した際に条件ロードで注入されていた。

```
bash .claude/scripts/py_invoke.sh -m pytest .claude/tests/rules/test_subprocess_encoding_convention.py -o addopts=""
```

**R2 の規範が、別の機構（TDD 内省パイプライン）を無音で殺すコマンドを配っていた。** 本日の遷移は復元不能。

### ANALYZED

本 retro で分析を実施。頻出パターン 0 件 / ルール候補 0 件（**遷移そのものが記録されていないため分析対象が存在しない**）。

---

## Step 3: KPT

### #1（先頭固定）: 実況形骸化 ガード 2 — **発火する**

原典 `docs/private/fable-l3-protocol.md` §5.4 が定める「過去 3 回分の実況第 1 文を並置し構文が同型なら形骸化」を、初めて実際に実行した。

#### 入力は 3 週間前から揃っていた

残置（ガード 4 の CH-1 証跡）は `docs/artifacts/` に **4 件実在**。最新は 2026-07-27。**持ち越しの理由は「入力がない」ではなかった。**

| 日付 | 成果物 | 実況第 1 文の骨格 |
|:---|:---|:---|
| 07-11 | `r-1-hooks-integration` | 見比べ **→** 気づいて **→** 手を止める |
| 07-11 | `r-1-usage-baseline` | 掴んだ後 → 飛んで → 確認しようとする **→ …対処した** |
| 07-15 | `r-1-final-audit-report` | 開く → 確認 → 確認 → 到達する **— このパスが成立するか** |
| 07-27 | `d-1-rationale` | 理解した直後 **→** 読みながら **→** 考え続けている |
| **08-17** | 段 2 承認要求 | 先に読み **→** 分かって **→** 探し始める |
| **08-17** | `/ship` Phase 3.5 | 打ち **→** 読んで **→** 書き始める |

#### 判定: 同型（ガード 2 発火）

**07-11 / 07-27 / 08-17 ×2 の 4 本が同一構文**である ——「受け手は A し、B して、C し始める（= 詰まる動詞で終わる）」の 3 連動作。直近 3 回に限っても同型であり、**ガード 2 の条件は満たされる**。

#### 副次発見: 検知器は劣化形を 1 つしか見ていない

実測された劣化は **3 形**あり、ガード 2 が検出するのは (a) だけである。

| | 劣化形 | 実例 |
|:-:|:---|:---|
| (a) | **同型構文化** | 上記 4 本 |
| (b) | **成果物の説明文化**（§5.2 が名指しした失敗そのもの） | `r-1-usage-baseline`（実況の途中で「…に明記して**対処した**」と書き手視点に転じている） |
| (c) | **検証項目化** | `r-1-final-audit-report`（矢印列挙 + 「このパスが**成立するか**」= 実況ではなくチェックリスト） |

つまり **6 本の残置のうち、実況として成立しているものは 0 本**である。(a) は形が固まっただけだが、(b)(c) は実況ですらない。

#### なぜ 3 回持ち越したのか（本議題の核）

入力があり、判定手段も原典が指定（`/retro` バッチで L3）していた。にもかかわらず 3 回逃げた。

**理由は「L1 が利害関係者だから」である。** ガード 2 の作業は「自分の直近の出力を並べて、自分が手を抜いていたかを自分で判定する」ことに等しい。他の議題（機構実装・envelope 修正・ゲート判定）は対象が外部にあり締切があるが、**#1 だけは対象が自分で、締切がなく、結論が自分に不利になりうる**。retro のたびに「他が具体的だったから」が成立し続けた。

これは memory `when-to-spend-adversarial-review` の「**L1 が利害関係者**」条件そのものであり、既知の型に該当していた。

### Keep

- **K1. 誕生ゲートの字義適用を、ユーザーの違和感が救った**: 段 2 で L1 は #1 を R5 に落としたが、これはゲートの出力（R4）を論拠で上書きしたものだった。ユーザーの「ハンドルの法則が読めない」がその上書きを検出した。**ゲートは正しく動いていて、L1 が迂回した**という構図が記録に残った
- **K2. 実装条件を「執行歯（テスト）」に変換した**: HGA #25 の 2 条件を散文で守らず `test_no_maintained_list_of_rule_files_in_source` / `test_main_does_not_inject` にした。**歯は実装中に実際に噛んだ**（説明コメント中の `rule-002` という引用を維持リストとして検出）
- **K3. append-only を選んだ判断が同日に正当化された**: 曝露ログを JSONL 追記型にした理由（「最後の 1 個を上書き」型は誤書込を後から区別できない）が、数時間後に `pre-compact-fired` の汚染で実証された
- **K4. 前提が消えた条項を「緩める」ではなく「壊した」**: 実 $ envelope は 2026-07-20 に前提を失っていたが、規律側は「圧力低下」と書いて軸を残していた。今回は軸ごと破棄した

### Problem

- **P1. 計器に 3 回触れて 2 回壊した**（本日）
  1. `pre-compact-fired` + SESSION_STATE の発火時刻 → スモークテストの環境変数名取り違え（`CLAUDE_PROJECT_DIR` / 正しくは `LAM_PROJECT_ROOT`）で偽の値を上書き。**復元済**
  2. `tdd-patterns.log` → `-o addopts=""` でデータ源を飢餓状態に。**復元不能**
  3. （セッション 20）retention 偽緑 = `rule-001` の確認手順を実行しないまま緑と見なした
- **P2. R2 規範が別機構を殺すコマンドを配っていた**（上記 Step 2.5）。**条件ロードの害は「消えること」だけではない** —— 生きている間に誤った処方を配ることもある。本日の機構 #7 は前者しか検出しない
- **P3. ガード 2 は発火し、かつ検知器が劣化形を 1/3 しか見ていない**（上記）
- **P4. ガード 2 の 3 回持ち越しは、入力不足ではなく L1 の利害関係が原因**（上記）
- **P5. 自律実行下の棄却案が残らない**: 棄却記録が拾えるのは「MAGI / HGA を回した判断」と「`/quick-save` 時点で L1 が覚えている分」のみ。`CLAUDE.md` §自律実行の既定「決まる場合は勝手に決めて実行」で落とした案は消え、**当の条項がその量を増やしている**
- **P6. 段 1 の実験設計欠陥**（セッション 20 / 既記録）: 答え合わせ Q&A 経路を汚染したまま実験を進めた。結論は注入内容の直接観測で出たため再実験はしない判断だが、設計としては欠陥
- **P7. HGA #25 で L1 の暫定判断が却下され、うち 2 点は利害相反の指摘だった**（セッション 20 / 既記録）。**P4 と同じ根**である —— L1 が自分に関わる判断を下すとき、結論が自己都合の方向に歪む

### Try

| # | 内容 | ゲート判定 | 宛先 |
|:-:|:---|:---|:---|
| **T1** | `subprocess-encoding-convention.md` §検証コマンドから **`-o addopts=""` を除去**（誤った処方の訂正） | 条項追加ではなくコマンド文字列の**訂正** | **PM 級編集**（入場ではない） |
| **T2** | ガード 2 の検知器を 3 劣化形（同型構文 / 説明文化 / 検証項目化）へ拡張 | 意味判断のため機構化不可（Step 1 NO）/ 検出イベント **1 件** | **R4 在庫**（入場は交換要のため保留） |
| **T3** | 計器に書き込みうる検証は、**隔離が効いていることを先に確認**してから実行 | 基質適合 **NO**（今日やらなかった）/ 検出イベント 1 件 | **R4 在庫** |
| **T4** | **#1 を retro の先頭に固定する運用を継続**（今回それで初めて実行できた） | 条項化しない（今回の実測を根拠として記録のみ） | 本ファイル |
| **T5** | P5 の検査 = 「決定と棄却案」枠が自律実行下の棄却を実際に拾えているか、次セッションで確認 | — | 次回 retro の入力 |

**T2・T3 を R1 に入れなかった理由**: いずれも交換相手を要する（1 対 1 / §A = 61）。かつ **P4 が示したのは「条項がないから実行されなかった」ではなく「利害関係があるから実行されなかった」** であり、条項を足しても同じ理由で無視される。**先頭固定（T4）という順序の操作の方が、条項 1 本より安い。**

---

## Step 4: アクション

| アクション | 反映先 | 優先度 |
|:---|:---|:---|
| T1: `-o addopts=""` の除去 | `.claude/rules/subprocess-encoding-convention.md`（**PM 級**） | **高**（機構を殺し続けるため） |
| T2 / T3 を在庫登録 | `docs/artifacts/clause-gate-ledger.md` §D | 中 |
| ガード 2 の発火結果を原典へ記録 | `docs/private/fable-l3-protocol.md` §5.4 | 中 |
| ANALYZED マーカー追記 | `.claude/tdd-patterns.log` | 高 |
| T5 を次回 retro 入力に | `SESSION_STATE.md` | 中 |

---

## 参照

- `docs/artifacts/clause-gate-ledger.md` §C 機構 #7 / §D 在庫
- `docs/artifacts/hga-summon-log.md` §25 詳細（要件 (ii) の裁定と実装条件）
- `docs/artifacts/upstream-intake-survey-2026-08-13.md` §B.4（段 1 の実測 = 機構 #7 の根拠）
- `docs/private/fable-l3-protocol.md` §5.4（ガード 2/4/5 の原典）
- memory `when-to-spend-adversarial-review`（P4・P7 の型）
